# 🧪 Локальное тестирование Mini App

## Проблема
Telegram Mini Apps работают только через HTTPS, а ваш локальный сервер на `http://localhost:8000`

## Решение: Туннель (ngrok или localtunnel)

---

## Вариант 1: ngrok (Рекомендуется) ⭐

### Установка:
1. Скачать с [ngrok.com](https://ngrok.com/download)
2. Зарегистрироваться (бесплатно)
3. Получить authtoken
4. Установить токен: `ngrok authtoken YOUR_TOKEN`

### Использование:

#### Терминал 1 - Запуск бота:
```powershell
# Запустить бот
cd C:\Users\User\Desktop\fudly-bot-main
& .venv\Scripts\python.exe bot.py
```

Бот запустится на `http://localhost:8000`

#### Терминал 2 - Запуск ngrok:
```powershell
# В новом окне PowerShell
ngrok http 8000
```

Вы увидите:
```
Forwarding https://abc123.ngrok-free.app -> http://localhost:8000
```

### Настройка:

#### 1. Обновить `.env`:
```env
# Локальный тест через ngrok
PARTNER_PANEL_URL=https://abc123.ngrok-free.app/partner-panel

# Другие настройки
TELEGRAM_BOT_TOKEN=ваш_токен
DATABASE_URL=postgresql://fudly:fudly123@localhost:5433/fudly
WEBHOOK_URL=
```

#### 2. Обновить `app.js`:
```javascript
// В app.js строка ~18
return 'https://abc123.ngrok-free.app/api';
```

#### 3. Открыть Mini App в боте:
1. Открыть бота в Telegram
2. Перейти в режим продавца
3. Нажать "🖥 Веб-панель"
4. Mini App откроется через ngrok туннель

---

## Вариант 2: localtunnel (Проще, но менее стабильно)

### Установка:
```powershell
npm install -g localtunnel
```

### Использование:

#### Терминал 1 - Бот:
```powershell
& .venv\Scripts\python.exe bot.py
```

#### Терминал 2 - Туннель:
```powershell
lt --port 8000 --subdomain fudly-test
```

URL будет: `https://fudly-test.loca.lt`

### Настройка:
```env
PARTNER_PANEL_URL=https://fudly-test.loca.lt/partner-panel
```

---

## Вариант 3: Dev Mode (Без туннеля, для разработки UI)

Если нужно только тестировать UI без Telegram auth:

### 1. Открыть в браузере:
```
file:///C:/Users/User/Desktop/fudly-bot-main/webapp/partner-panel/index.html
```

### 2. Ввести Telegram ID:
При первом открытии появится prompt:
```
Enter your Telegram ID for testing: 123456789
```

Ваш ID можно узнать через бота: `/start` → "👤 Профиль"

### 3. API будет работать:
```javascript
// app.js автоматически определит localhost
const API_BASE_URL = 'http://localhost:8000/api';
```

### ⚠️ Ограничения Dev Mode:
- ❌ Telegram WebApp API недоступно
- ❌ initData не работает (используется мок)
- ✅ Все API endpoints работают
- ✅ Можно тестировать UI/UX
- ✅ CRUD товаров работает

---

## Рекомендуемый процесс разработки:

### Для UI/UX:
1. Открыть `index.html` в браузере
2. Использовать Dev Mode
3. Тестировать визуал и функции

### Для полного теста:
1. Запустить бота локально
2. Запустить ngrok
3. Обновить `PARTNER_PANEL_URL` и `app.js`
4. Открыть через бота в Telegram
5. Полноценный тест с auth

---

## 🚀 Quick Start (ngrok)

```powershell
# Терминал 1: База данных (если не запущена)
docker-compose up -d

# Терминал 2: Бот
cd C:\Users\User\Desktop\fudly-bot-main
& .venv\Scripts\python.exe bot.py

# Терминал 3: ngrok
ngrok http 8000
# Скопировать URL: https://abc123.ngrok-free.app

# Терминал 4: Обновить конфигурацию
# Редактировать .env:
# PARTNER_PANEL_URL=https://abc123.ngrok-free.app/partner-panel

# Редактировать webapp/partner-panel/app.js (строка ~18):
# return 'https://abc123.ngrok-free.app/api';

# Перезапустить бота (Ctrl+C в Терминал 2, потом снова запустить)
& .venv\Scripts\python.exe bot.py

# Готово! Открыть бота → "🖥 Веб-панель"
```

---

## Troubleshooting

### ngrok Warning "Visit Site"
При первом открытии ngrok показывает предупреждение.
**Решение:** Нажать "Visit Site" один раз.

### 401 Unauthorized
- Проверить что используете ngrok URL (не localhost)
- Проверить что бот запущен
- Перезапустить бота после изменения .env

### CORS errors
- Проверить что в `api_server.py` есть ngrok домен:
```python
allow_origins=[
    "https://*.ngrok-free.app",  # Добавить эту строку
    "https://fudly-partner-panel.vercel.app",
    # ...
]
```

### Туннель отключается
- ngrok free имеет таймауты → переподключиться
- Использовать платную версию ngrok
- Или использовать localtunnel как альтернативу

---

## Альтернатива: Railway Preview

Если не хочется возиться с туннелями:

1. Push код в git
2. Создать Railway project
3. Deploy
4. Получить автоматический HTTPS URL
5. Использовать для разработки

```bash
railway up
railway logs --tail
```

URL будет: `https://your-project.up.railway.app`

---

## Сравнение методов:

| Метод | Плюсы | Минусы | Для чего |
|-------|-------|--------|----------|
| **Dev Mode (Browser)** | Быстро, без настройки | Нет Telegram auth | UI/UX разработка |
| **ngrok** | Стабильно, HTTPS, полный функционал | Установка, регистрация | Полное тестирование |
| **localtunnel** | Быстрая установка | Менее стабильно | Быстрый тест |
| **Railway Preview** | Реальное окружение | Медленный deploy | Финальная проверка |

---

## Рекомендация для вас:

### Сейчас (разработка):
```powershell
# 1. Установить ngrok
# https://ngrok.com/download

# 2. Зарегистрироваться и получить токен
ngrok authtoken YOUR_TOKEN

# 3. Запустить бота
& .venv\Scripts\python.exe bot.py

# 4. В новом терминале:
ngrok http 8000

# 5. Скопировать URL и обновить конфиги
```

### Для продакшена:
- Vercel для Mini App статики
- Railway для бота
- Никаких туннелей не нужно

Готово! 🎉
