# Диагностика проблемы с флоу доставки

## Проблема:
После ввода адреса и отправки чека оплаты флоу не продолжается.

## Проверенные места:

### 1. Обработчик ввода адреса (`dlv_address_input`)
**Файл:** `handlers/customer/orders/delivery.py:497`
- ✅ Адрес сохраняется в state: `await state.update_data(address=text)`
- ✅ Состояние меняется на `OrderDelivery.payment_method_select`
- ✅ Отправляется карточка с выбором способа оплаты

### 2. Обработчик выбора оплаты картой (`dlv_pay_card`)
**Файл:** `handlers/customer/orders/delivery.py:697`
- ✅ payment_method сохраняется в state
- ✅ Состояние меняется на `OrderDelivery.payment_proof`
- ✅ Удаляется предыдущее сообщение
- ✅ Показываются детали карты для оплаты

### 3. Обработчик загрузки чека (`dlv_payment_proof`)
**Файл:** `handlers/customer/orders/delivery.py:792`

**ПРОБЛЕМА НАЙДЕНА!**
```python
@router.message(OrderDelivery.payment_proof, F.photo)
async def dlv_payment_proof(message: types.Message, state: FSMContext) -> None:
```

Изначально в сигнатуре функции **отсутствовал параметр `db: DatabaseProtocol`**.

Это означало, что:
1. При вызове обработчика middleware передавал `db`, но функция не принимала этот параметр
2. Код использовал глобальную переменную `db` из модуля
3. Если middleware не передавал db (или был конфликт параметров), обработчик мог не срабатывать

## Исправления:

### 1. ✅ Добавлен параметр `db` в сигнатуру:
```python
async def dlv_payment_proof(message: types.Message, state: FSMContext, db: DatabaseProtocol) -> None:
```

### 2. ✅ Добавлено логирование:
- При вводе адреса
- При выборе способа оплаты
- При загрузке чека
- При проверке данных перед созданием заказа

### 3. ✅ Улучшена проверка данных:
```python
if not all(k in data for k in required):
    missing = [k for k in required if k not in data]
    logger.error(f"❌ User {user_id} missing required data: {missing}")
    logger.debug(f"Available data keys: {list(data.keys())}")
```

## Как протестировать:

1. **Запустите бота** и проверьте логи
2. **Попробуйте создать заказ с доставкой:**
   - Выберите товар
   - Нажмите "🚚 Доставка"
   - Введите количество
   - Введите адрес (например: "Чиланзар, 5-массив, дом 10")
   - Выберите "💳 Karta" (Оплата картой)
   - Загрузите скриншот чека

3. **Проверьте логи на наличие:**
   ```
   ✅ User 123456789 entered delivery address: Чиланзар, 5-массив, дом 10...
   🔄 User 123456789 moved to payment method selection state
   💳 User 123456789 selected card payment
   🔄 User 123456789 state set to payment_proof, awaiting screenshot
   💳 Showing card payment details for user (store_id=1)
   📸 User 123456789 uploaded payment screenshot for delivery order
   ```

4. **Если проблема сохраняется:**
   - Проверьте, вызывается ли обработчик `dlv_payment_proof`
   - Проверьте, что в state сохранены все требуемые поля: `offer_id`, `store_id`, `quantity`, `address`
   - Посмотрите на ошибки в логах

## Возможные дополнительные проблемы:

### Проблема с middleware:
Если `DbSessionMiddleware` не передает `db` в обработчики правильно, проверьте:
```python
# bot.py
dp.update.middleware(DbSessionMiddleware(db))
```

### Проблема с порядком роутеров:
Убедитесь, что роутер orders регистрируется правильно:
```python
# bot.py:466
dp.include_router(orders_router)  # Includes delivery_admin.router internally
```

### Проблема с состоянием FSM:
Убедитесь, что состояние `OrderDelivery.payment_proof` определено:
```python
# handlers/common/states.py:165
class OrderDelivery(StatesGroup):
    offer_id = State()
    quantity = State()
    address = State()
    payment_method_select = State()
    payment_proof = State()  # ← Это состояние должно быть определено
```

## Итог:
Основная проблема была в отсутствующем параметре `db` в сигнатуре функции `dlv_payment_proof`. После исправления флоу должен работать корректно.
