# Fudly Telegram Bot - Core Dependencies

# Telegram Bot Framework (REQUIRED)
aiogram>=3.0.0

# Environment variables (REQUIRED)
python-dotenv>=0.19.0

# Web server for Render deployment (REQUIRED for Render)
aiohttp>=3.8.0
flask>=2.3.0

# Optional dependencies (remove if not needed)
# redis>=4.5.0                   # For Redis caching (optional, fallback to in-memory)
# python-json-logger>=2.0.0      # For JSON logging (optional, fallback to basic logging)
# Pillow>=10.0.0                 # For image processing (optional)

# Development and testing (optional)
# pytest>=7.0.0                  # Testing framework  
# pytest-asyncio>=0.21.0         # Async testing support

# Security & Input Validation (recommended)
# bleach>=6.0.0                   # HTML sanitization

# HTTP client for health checks
requests>=2.28.0

# Database connection pooling (alternative to our custom pool)
# sqlalchemy>=2.0.0  # uncomment if switching to SQLAlchemy

# База данных (встроена в Python, но указываем для информации)
# sqlite3 - встроен в Python

# Асинхронность (встроена в Python 3.8+)
# asyncio - встроен в Python