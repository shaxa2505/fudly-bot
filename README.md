# 🍽️ Fudly Bot - Спасайте еду, экономьте деньги

[![Railway](https://railway.app/button.svg)](https://railway.app/template/fudly-bot)

**Fudly** - это Telegram бот-аналог Too Good To Go для Узбекистана. Помогает бизнесу продавать еду со скидкой до 70% вместо выбрасывания, а покупателям - экономить деньги и заботиться об экологии.

## ⚡ Быстрый старт - 5 минут

### Вариант 1: Deploy to Railway (1 клик)

1. Нажмите кнопку [![Deploy on Railway](https://railway.app/button.svg)](https://railway.app/new/template?template=https://github.com/shaxa2505/fudly-bot)

2. Получите токен бота у [@BotFather](https://t.me/BotFather):
   ```
   /newbot → Следуйте инструкциям → Скопируйте токен
   ```

3. Получите свой Telegram ID у [@userinfobot](https://t.me/userinfobot):
   ```
   /start → Скопируйте ID
   ```

4. Вставьте в Railway переменные:
   - `TELEGRAM_BOT_TOKEN` - ваш токен
   - `ADMIN_ID` - ваш ID
   - `USE_WEBHOOK` - true
   - `WEBHOOK_URL` - https://your-app.railway.app

5. Создайте Volume для БД:
   - Storage → New Volume
   - Mount Path: `/data`
   - Добавьте переменную: `DATABASE_PATH=/data/fudly.db`

6. Напишите боту `/start` в Telegram!

**� Подробнее:** [QUICK_START.md](QUICK_START.md)

### Вариант 2: Локальный запуск

```bash
# Клонируйте репозиторий
git clone https://github.com/shaxa2505/fudly-bot.git
cd fudly-bot

# Установите зависимости
pip install -r requirements.txt

# Настройте .env
cp .env.example .env
# Отредактируйте .env (добавьте TELEGRAM_BOT_TOKEN и ADMIN_ID)

# Запустите
python bot.py
```

---

## 🎯 Возможности

### Для покупателей 🛍️
- ✅ Просмотр доступных предложений со скидкой до 70%
- ✅ Фильтрация по категориям (хлеб, молочное, мясо и т.д.)
- ✅ Бронирование товаров с кодом получения
- ✅ Рейтинги и отзывы магазинов
- ✅ Избранные магазины
- ✅ История бронирований
- ✅ Двуязычный интерфейс (Русский/Узбекский)

### Для партнёров 🏪
- ✅ Создание предложений за 1 минуту
- ✅ Массовое создание товаров
- ✅ Загрузка фото
- ✅ Управление бронированиями
- ✅ Подтверждение выдачи по коду
- ✅ Детальная аналитика:
  - Статистика бронирований
  - Конверсия
  - Популярные категории
  - Анализ по дням недели
  - Средний рейтинг

### Для администратора 👑
- ✅ Модерация новых магазинов
- ✅ Одобрение/отклонение заявок
- ✅ Статистика по всей платформе
- ✅ Экспорт данных в CSV
- ✅ Управление пользователями

---

## 📊 Технологии

- **Python 3.11** - современный, быстрый
- **aiogram 3.x** - асинхронный Telegram Bot framework
- **SQLite** - встроенная БД с connection pooling
- **Railway** - простой деплой и хостинг
- **Webhook** - мгновенная доставка сообщений

### Оптимизация:
- 🚀 **13 индексов БД** для быстрых запросов
- ⚡ **Connection pooling** для эффективной работы с БД
- 🔄 **Webhook вместо polling** в production
- 🛡️ **Валидация данных** и защита от SQL injection
- 📊 **Health check endpoints** для мониторинга

---

## 📁 Структура проекта

```
fudly-bot/
├── bot.py                 # Главный файл бота (3000+ строк)
├── database.py            # База данных с оптимизацией
├── keyboards.py           # UI клавиатуры
├── localization.py        # Тексты RU/UZ
├── requirements.txt       # Зависимости
├── Procfile              # Railway deployment
├── runtime.txt           # Python версия
├── .env.example          # Шаблон настроек
│
├── QUICK_START.md        # ⚡ Быстрый старт
├── RAILWAY_DEPLOY.md     # 📘 Деплой на Railway
├── CHECKLIST.md          # ✅ Тестирование
├── SUMMARY.md            # 📖 Полное описание
│
├── deploy.ps1            # 🪟 Авто-деплой (Windows)
└── deploy.sh             # 🐧 Авто-деплой (Linux/Mac)
```

---

## ✅ Исправленные проблемы

### 1. **Товар не исчезает после бронирования**
- ✅ **Исправлено**: `update_offer_quantity()` теперь автоматически деактивирует товар когда `quantity = 0`
- ✅ **Добавлено**: Автоматическая реактивация товара при отмене бронирования
- ✅ **Логика**: При бронировании quantity уменьшается, при отмене - увеличивается

### 2. **Отключение уведомлений не работало**
- ✅ **Исправлено**: Добавлен обработчик `toggle_notifications`
- ✅ **Функционал**: Переключение уведомлений с обновлением клавиатуры настроек
- ✅ **База данных**: Метод `db.toggle_notifications()` работает корректно

### 3. **Удаление аккаунта не работало**
- ✅ **Исправлено**: Добавлены 3 обработчика:
  - `delete_account` - показ предупреждения
  - `confirm_delete_yes` - подтверждение удаления
  - `confirm_delete_no` - отмена удаления
- ✅ **Безопасность**: Двойное подтверждение перед удалением
- ✅ **Полная очистка**: Удаляются все данные пользователя (магазины, товары, бронирования, рейтинги)

### 4. **Ошибка IndexError в "Мои бронирования"**
- ✅ **Исправлено**: Обновлен SQL запрос `get_user_bookings()` для включения всех необходимых полей
- ✅ **Добавлено**: `available_until`, `city` в результат запроса
- ✅ **Индексы**: Обновлены все индексы в коде для соответствия структуре данных

### 5. **Старый синтаксис aiogram 2.x**
- ✅ **Исправлено**: Заменен `InlineKeyboardMarkup` на `InlineKeyboardBuilder` (aiogram 3.x)
- ✅ **Совместимость**: Все клавиатуры теперь используют правильный синтаксис

### 6. **Оптимизация проекта**
- ✅ **Удалено**: 22 старых файла документации (~200 КБ)
- ✅ **Очищено**: Backup файлы и временные скрипты
- ✅ **Результат**: Проект ускорен и упрощен

## 📊 Текущее состояние

### Файлы проекта (основные):
- `bot.py` (54 KB) - Главный файл бота
- `database.py` (27 KB) - База данных
- `keyboards.py` (8 KB) - Клавиатуры
- `localization.py` (23 KB) - Переводы
- `fudly.db` (32 KB) - SQLite база
- `.env` - Настройки (TOKEN, ADMIN_ID)

### Статистика базы:
- Пользователей: 3
- Активных предложений: 1
- Всего бронирований: 4

## 🎯 Полная функциональность

### Для покупателей:
- ✅ Просмотр доступных предложений
- ✅ Бронирование товаров
- ✅ Просмотр своих бронирований
- ✅ Отмена бронирования (товар возвращается)
- ✅ Смена города
- ✅ Профиль с настройками
- ✅ Отключение уведомлений
- ✅ Удаление аккаунта

### Для партнеров:
- ✅ Регистрация магазина
- ✅ Создание предложений
- ✅ Массовое создание предложений
- ✅ Просмотр своих товаров
- ✅ Дублирование/удаление товаров
- ✅ Подтверждение выдачи по коду
- ✅ Просмотр статистики магазина
- ✅ Переключение между режимами

### Для админов:
- ✅ Модерация заявок на партнерство
- ✅ Статистика системы
- ✅ Управление пользователями
- ✅ Рассылка уведомлений

## 🔧 Технические улучшения

### Database.py
```python
# Автоматическое управление статусом товара
def update_offer_quantity(self, offer_id: int, new_quantity: int):
    if new_quantity <= 0:
        # Деактивация при окончании
        cursor.execute('UPDATE offers SET quantity = 0, status = ? WHERE offer_id = ?', 
                      ('inactive', offer_id))
    else:
        # Реактивация при возврате
        cursor.execute('UPDATE offers SET quantity = ?, status = ? WHERE offer_id = ?', 
                      (new_quantity, 'active', offer_id))
## 🛠️ Standalone booking expiry worker

Для надёжной работы напоминаний и авто-отмен (вне процесса бота) можно запускать отдельный worker process.

1) Запуск (локально или в контейнере):

```powershell
$env:TELEGRAM_BOT_TOKEN = '<your_token>'
# Для Postgres в staging/production
$env:DATABASE_URL = 'postgresql://user:pass@host:port/dbname'
python scripts/run_booking_worker.py
```

2) systemd unit (пример):

```ini
[Unit]
Description=Fudly Booking Expiry Worker
After=network.target

[Service]
User=www-data
WorkingDirectory=/opt/fudly-bot
Environment=TELEGRAM_BOT_TOKEN=your_token
Environment=DATABASE_URL=postgresql://...    
ExecStart=/opt/fudly-bot/.venv/bin/python /opt/fudly-bot/scripts/run_booking_worker.py
Restart=always

[Install]
WantedBy=multi-user.target
```

3) Heroku / Procfile example:

```
worker: .venv/bin/python scripts/run_booking_worker.py
```

4) Notes:
- Скрипт использует `TELEGRAM_BOT_TOKEN` для отправки напоминаний; без токена worker не стартует.
- Для Postgres укажите `DATABASE_URL`; иначе используется локальная SQLite (как fallback).
- Скрипт корректно закрывает DB-пул и сессию бота при остановке.

```

### Bot.py
```python
# Проверка статуса перед отменой
@dp.callback_query(F.data.startswith("cancel_booking_"))
async def cancel_booking(callback: types.CallbackQuery):
    if booking and booking[3] == 'pending':  # Только активные
        db.cancel_booking(booking_id)
        db.update_offer_quantity(booking[1], offer[6] + 1)  # Возврат товара
```

## 🚀 Запуск бота

```powershell
# Остановить все процессы Python
taskkill /F /IM python.exe /T 2>$null

# Запустить бота
python bot.py
```

## 📝 Заметки

1. **Бронирование**: Товар автоматически исчезает когда заканчивается и появляется при отмене
2. **Уведомления**: Можно отключить в настройках профиля
3. **Удаление**: Требует двойного подтверждения, удаляет все данные безвозвратно
4. **Производительность**: Проект очищен от мусора, работает быстро
5. **Стабильность**: Все ошибки исправлены, бот работает без сбоев

---

**Последнее обновление**: 31 октября 2025
**Версия**: 3.0 (Финальная)
**Статус**: ✅ Полностью рабочий

---

## 📣 Booking expiry worker (standalone)

We provide a standalone worker to run reminders and auto-cancel expired bookings outside the bot process. This is recommended for production to isolate background work.

Usage (Windows PowerShell):

```powershell
$env:TELEGRAM_BOT_TOKEN = '<your_token>'
$env:DATABASE_URL = '<your_database_url>'
python .\scripts\booking_expiry_worker.py
```

Behavior:
- Sends a localized reminder (RU/UZ) 1 hour before booking expiry, marks `reminder_sent` to avoid duplicates.
- Cancels bookings past `expiry_time` and returns reserved quantity to the offer atomically.

Notes:
- The bot still starts an internal worker by default, but running the standalone script is safer and recommended on production hosts.
