# ✅ ДОБАВЛЕНО: Переключение обратно на режим партнера

**Дата:** 16 ноября 2025  
**Статус:** ✅ ГОТОВО

---

## 🎯 Что добавлено

Теперь партнеры могут переключаться между режимами:
- **Партнер → Покупатель** (было)
- **Покупатель → Партнер** ✨ (новое!)

---

## 🔄 Как работает

### В профиле партнера:

1. **Режим партнера** → показывает кнопку "🛒 Режим покупателя"
2. **Режим покупателя** → показывает кнопку "🏪 Режим партнера"

### Защита:
- ✅ Только партнеры (role="seller") могут переключиться на режим партнера
- ✅ Обычные пользователи увидят ошибку

---

## 📝 Изменения

### 1. `app/keyboards/user.py`

Обновлена `settings_keyboard()`:
```python
def settings_keyboard(
    notifications_enabled: bool, 
    lang: str = 'ru', 
    role: str | None = None,
    current_mode: str = 'customer'  # ✨ Новый параметр
) -> InlineKeyboardMarkup:
```

**Логика:**
```python
if role == 'seller':
    if current_mode == 'seller':
        # Показать "🛒 Режим покупателя"
    else:
        # Показать "🏪 Режим партнера"
else:
    # Показать "Стать партнером"
```

### 2. `handlers/user/profile.py`

**Добавлено:**
- ✅ `switch_to_seller_cb()` - callback handler для кнопки
- ✅ Определение `current_mode` в функции `profile()`
- ✅ Передача `current_mode` во все вызовы `settings_keyboard()`

**Проверка прав:**
```python
if not user or user.role != "seller":
    await callback.answer(
        "Только партнеры могут использовать этот режим",
        show_alert=True
    )
    return
```

### 3. `handlers/common_user.py`

**Добавлено:**
- ✅ `switch_to_seller()` - text handler для текстовой команды
- ✅ `main_menu_seller` в зависимости
- ✅ Обновлена `setup()` функция

### 4. `bot.py`

Обновлен вызов:
```python
common_user.setup(
    bot, db, user_view_mode, get_text, 
    main_menu_customer, booking_filters_keyboard,
    main_menu_seller  # ✨ Добавлено
)
```

---

## 🧪 Как проверить

### Для партнера:

1. **Откройте профиль** → нажмите 👤 Профиль
2. **В режиме партнера** увидите кнопку "🛒 Режим покупателя"
3. **Нажмите** → меню изменится на меню покупателя
4. **Откройте профиль снова** → увидите кнопку "🏪 Режим партнера"
5. **Нажмите** → вернетесь в меню партнера

### Для обычного пользователя:

1. Откройте профиль
2. Увидите кнопку "🤝 Стать партнером" (не изменилось)

---

## 📊 Результат

**До:**
- ✅ Партнер → Покупатель (работало)
- ❌ Покупатель → Партнер (не работало)

**После:**
- ✅ Партнер → Покупатель
- ✅ Покупатель → Партнер ✨

---

## 🎨 UX улучшения

### Смарт-кнопка в профиле:
- Показывает только актуальное действие
- Не загромождает интерфейс
- Понятно что произойдет при нажатии

### Сообщения:
- **RU:** "Переключено в режим партнера"
- **UZ:** "Hamkor rejimiga o'tkazildi"

---

## ✅ Готово к тестированию!

Запустите бот и проверьте переключение режимов:
```bash
python bot.py
```

**Теперь партнеры могут свободно переключаться между режимами!** 🎉
