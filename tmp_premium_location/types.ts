
export type AddressDetails = {
  entrance: string;
  floor: string;
  apt: string;
  note: string;
};

export type NavItem = 'browse' | 'saved' | 'cart' | 'orders' | 'profile';

export type Category = 'home' | 'work' | 'yunusabad';
