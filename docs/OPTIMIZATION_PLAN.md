# 🎯 План оптимизации проекта Fudly Bot

**Дата:** 8 декабря 2025
**Статус:** Начальная фаза анализа
**Цель:** Упростить поддержку, уменьшить дублирование, улучшить читаемость

---

## 📊 Текущее состояние

### Статистика кода

**Самые большие файлы (требуют разбиения):**
- `handlers/customer/offers/browse.py` - **1448 строк** ⚠️
- `handlers/customer/cart/router.py` - **1296 строк** ⚠️
- `handlers/bookings/customer.py` - **1275 строк** ⚠️
- `handlers/seller/store_settings.py` - **1231 строки** ⚠️
- `handlers/admin/dashboard.py` - **1010 строк** ⚠️

**Итого:** ~25 файлов >300 строк, сложно поддерживать

### Проблемы архитектуры

1. **Дублирование кода:**
   - Похожие функции создания заказов в 6 местах
   - Одинаковые UI builders в разных модулях
   - Дублированная логика подтверждения заказов
   - Повторяющиеся обработчики payment

2. **Неясная ответственность:**
   - `cart/router.py` делает всё: UI + логика + БД + уведомления
   - `bookings/customer.py` смешивает UI и бизнес-логику
   - Обработчики напрямую работают с БД

3. **Сложность тестирования:**
   - Код сильно связан (tight coupling)
   - Много глобальных переменных (`db`, `bot`)
   - Нет разделения на слои

4. **Трудность навигации:**
   - Много файлов без четкой структуры
   - Непонятно где что искать
   - Нет карты зависимостей

---

## 🎯 Цели оптимизации

### Краткосрочные (1-2 недели)
✅ **Цель 1:** Документация и карта проекта
✅ **Цель 2:** Удалить мёртвый код
✅ **Цель 3:** Объединить дублированный код
✅ **Цель 4:** Разбить большие файлы

### Среднесрочные (3-4 недели)
⏳ **Цель 5:** Разделить на слои (UI, Services, Repositories)
⏳ **Цель 6:** Унифицировать через OrderService
⏳ **Цель 7:** Добавить тесты

### Долгосрочные (1-2 месяца)
🔮 **Цель 8:** Полная модульность
🔮 **Цель 9:** CI/CD pipeline
🔮 **Цель 10:** Мониторинг и метрики

---

## 📋 Этап 1: Документация (СЕЙЧАС - 1-2 дня)

### 1.1 Создать архитектурную карту

```
PROJECT_MAP.md - карта модулей:
├── handlers/
│   ├── common/          ➜ Регистрация, команды, утилиты
│   ├── bookings/        ➜ Заказы на самовывоз (pickup)
│   ├── customer/        ➜ Клиентские функции
│   │   ├── cart/        ➜ Корзина покупок
│   │   ├── offers/      ➜ Каталог и поиск
│   │   └── orders/      ➜ Доставка (delivery)
│   ├── seller/          ➜ Функции продавца
│   └── admin/           ➜ Админ панель
├── app/
│   ├── services/        ➜ Бизнес-логика
│   ├── repositories/    ➜ Работа с БД
│   └── api/             ➜ WebApp API
└── database_pg_module/  ➜ PostgreSQL драйвер
```

### 1.2 Описать за что отвечает каждый модуль

**handlers/bookings/** - Заказы на самовывоз
- `customer.py` - Клиент создаёт/отменяет бронь
- `partner.py` - Продавец подтверждает/отклоняет
- `booking_ui.py` - UI карточки броней
- `utils.py` - Общие функции

**handlers/customer/cart/** - Корзина
- `router.py` - Главный роутер (НУЖНО РАЗБИТЬ)
- `cart_ui.py` - UI builders
- `cart_checkout.py` - Оформление заказа
- `storage.py` - In-memory хранилище корзины

**handlers/customer/orders/** - Доставка
- `delivery.py` - Флоу создания заказа
- `delivery_ui.py` - UI карточки заказов
- `delivery_partner.py` - Подтверждение продавцом
- `delivery_admin.py` - Подтверждение оплаты админом
- `history.py` - История заказов

### 1.3 Создать матрицу дублирования

| Функциональность | Место 1 | Место 2 | Место 3 | Действие |
|-----------------|---------|---------|---------|----------|
| Создание заказа | bookings/customer.py | cart/router.py | orders/delivery.py | → Унифицировать через OrderService |
| UI карточка заказа | bookings/customer.py | cart/cart_ui.py | orders/delivery_ui.py | → Общий UI builder |
| Уведомления партнёру | bookings/customer.py | cart/router.py | orders/delivery.py | → app/services/notifications.py |
| Обработка оплаты | cart/router.py | orders/delivery.py | payments.py | → app/services/payment_service.py |

---

## 📋 Этап 2: Быстрые улучшения (3-5 дней)

### 2.1 Удалить мёртвый код

**Кандидаты на удаление:**
- ❌ `handlers/admin/legacy.py` - старый код
- ❌ Закомментированный код в файлах
- ❌ Неиспользуемые функции (найти через grep)

**Команда для поиска:**
```bash
# Найти закомментированный код
grep -r "^#.*def\|^#.*async def" handlers/

# Найти неиспользуемые функции
# (нужен анализ импортов)
```

### 2.2 Разбить большие файлы

#### 2.2.1 `handlers/customer/cart/router.py` (1296 строк)
**Разбить на:**
```
cart/
├── router.py              # Основной роутер (50-100 строк)
├── cart_view.py           # Показ корзины (200 строк)
├── cart_actions.py        # Добавить/удалить товар (300 строк)
├── cart_checkout.py       # ✅ УЖЕ ЕСТЬ (582 строки)
├── cart_ui.py             # ✅ УЖЕ ЕСТЬ (200 строк)
└── storage.py             # ✅ УЖЕ ЕСТЬ
```

#### 2.2.2 `handlers/bookings/customer.py` (1275 строк)
**Разбить на:**
```
bookings/
├── router.py              # Основной роутер
├── customer_create.py     # Создание брони
├── customer_view.py       # Просмотр броней
├── customer_actions.py    # Отмена/рейтинг
├── partner.py             # ✅ УЖЕ ЕСТЬ
├── booking_ui.py          # ✅ УЖЕ ЕСТЬ
└── utils.py               # ✅ УЖЕ ЕСТЬ
```

#### 2.2.3 `handlers/customer/offers/browse.py` (1448 строк)
**Разбить на:**
```
offers/
├── router.py              # Основной роутер
├── browse.py              # Каталог (упростить до 400 строк)
├── browse_helpers.py      # ✅ УЖЕ ЕСТЬ
├── search.py              # ✅ УЖЕ ЕСТЬ
├── offer_card.py          # UI карточки товара
└── favorites.py           # Избранное (уже отдельно)
```

### 2.3 Извлечь общий код

#### 2.3.1 Создать `app/services/order_service.py`
✅ **УЖЕ ЕСТЬ** - продолжить расширение

**Добавить методы:**
```python
class OrderService:
    async def create_pickup_booking()      # ✅ Есть
    async def create_delivery_order()      # ✅ Есть
    async def create_cart_booking()        # TODO
    async def create_cart_delivery()       # TODO
    async def validate_order()             # TODO
    async def calculate_totals()           # TODO
```

#### 2.3.2 Создать `app/services/notification_service.py`

```python
class NotificationService:
    async def notify_partner_new_order()
    async def notify_customer_confirmed()
    async def notify_customer_rejected()
    async def notify_admin_payment()
```

#### 2.3.3 Создать `app/ui/order_cards.py`

```python
def build_order_card(order_data: dict, lang: str) -> tuple[str, InlineKeyboardMarkup]:
    """Универсальный builder для всех типов заказов."""
    pass

def build_booking_card(booking_data: dict, lang: str) -> tuple[str, InlineKeyboardMarkup]:
    """Builder для броней."""
    pass
```

---

## 📋 Этап 3: Архитектурные улучшения (1-2 недели)

### 3.1 Разделить на слои

**Новая структура:**
```
app/
├── domain/              # Модели данных
│   ├── entities/
│   │   ├── order.py
│   │   ├── booking.py
│   │   └── user.py
│   └── value_objects/
│       ├── price.py
│       └── address.py
│
├── services/            # Бизнес-логика
│   ├── order_service.py         # ✅ Есть
│   ├── booking_service.py       # TODO
│   ├── notification_service.py  # TODO
│   ├── payment_service.py       # TODO
│   └── cart_service.py          # TODO
│
├── repositories/        # Работа с БД
│   ├── order_repository.py      # ✅ Частично есть
│   ├── booking_repository.py    # ✅ Есть
│   ├── user_repository.py       # ✅ Есть
│   └── offer_repository.py      # ✅ Есть
│
├── ui/                  # UI builders (новая папка)
│   ├── cards/
│   │   ├── order_card.py
│   │   ├── booking_card.py
│   │   └── offer_card.py
│   └── keyboards/
│       ├── order_keyboards.py
│       └── booking_keyboards.py
│
└── use_cases/          # Сценарии использования (новое)
    ├── create_order.py
    ├── confirm_order.py
    └── cancel_order.py
```

### 3.2 Убрать глобальные переменные

**Было:**
```python
# В каждом файле
db: Any = None
bot: Any = None

def setup_dependencies(database, bot_instance):
    global db, bot
    db = database
    bot = bot_instance
```

**Стало:**
```python
# Dependency injection через конструктор или middleware
class OrderHandler:
    def __init__(self, db: DatabaseProtocol, bot: Bot):
        self.db = db
        self.bot = bot
```

### 3.3 Унифицировать создание заказов

**Все 6 точек входа → один метод:**
```python
# Было (6 мест):
# 1. handlers/bookings/customer.py:1067 - create_booking()
# 2. handlers/customer/cart/router.py:328 - cart_confirm_pickup()
# 3. handlers/customer/cart/router.py:469 - cart_confirm_delivery()
# 4. handlers/customer/orders/delivery.py - delivery order
# 5. app/api/webapp_api.py - через API
# 6. app/services/order_service.py - через сервис

# Стало (1 место):
from app.services.order_service import OrderService

order_service = OrderService(db, bot)
result = await order_service.create_order(
    user_id=user_id,
    items=items,
    delivery_method="pickup",  # или "delivery"
    address=address if delivery else None
)
```

---

## 📋 Этап 4: Тестирование (5-7 дней)

### 4.1 Unit тесты для сервисов

```python
# tests/services/test_order_service.py
async def test_create_pickup_order():
    service = OrderService(mock_db, mock_bot)
    result = await service.create_order(...)
    assert result.success
    assert result.order_id > 0
```

### 4.2 Integration тесты для flows

```python
# tests/flows/test_booking_flow.py
async def test_complete_booking_flow():
    # 1. User starts booking
    # 2. Enters quantity
    # 3. Confirms
    # 4. Check booking created
    # 5. Partner confirms
    # 6. Check notifications sent
```

### 4.3 Настроить CI/CD

```yaml
# .github/workflows/test.yml
name: Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run tests
        run: pytest tests/
      - name: Check types
        run: mypy .
      - name: Check formatting
        run: ruff check .
```

---

## 📊 Метрики успеха

### Количественные
- ❌ **Сейчас:** 25 файлов >300 строк
- ✅ **Цель:** <10 файлов >300 строк

- ❌ **Сейчас:** 6 точек создания заказов
- ✅ **Цель:** 1 точка (OrderService)

- ❌ **Сейчас:** 0% покрытие тестами
- ✅ **Цель:** >50% покрытие для services

### Качественные
- ✅ Можно быстро найти нужный код
- ✅ Понятно где что менять
- ✅ Легко добавлять новые фичи
- ✅ Можно безопасно рефакторить

---

## 🚀 Приоритеты на эту неделю

### День 1-2: Документация
- [x] ~~Создать OPTIMIZATION_PLAN.md~~
- [ ] Создать PROJECT_MAP.md
- [ ] Создать RESPONSIBILITY_MATRIX.md

### День 3-4: Быстрые улучшения
- [ ] Удалить legacy код
- [ ] Разбить cart/router.py на модули
- [ ] Извлечь общий UI builder

### День 5: Тестирование
- [ ] Проверить что всё работает
- [ ] Создать коммит
- [ ] Задеплоить

---

## 📝 Следующие шаги

**Что делаем дальше?**

1. **Создать PROJECT_MAP.md** - подробная карта проекта
2. **Разбить cart/router.py** - самый большой файл
3. **Создать NotificationService** - убрать дублирование
4. **Добавить тесты** - начать с OrderService

**Ваше решение?**
- Начать с карты проекта?
- Сразу разбивать большие файлы?
- Сначала тесты?
