# 🚀 Инструкция по внедрению улучшений мини-приложения

## Что было сделано в бэкенде

### ✅ Обновлен API (`app/api/webapp_api.py`)

Добавлены новые endpoints:

1. **Расширенные фильтры для товаров**
   - `GET /api/v1/offers` - теперь поддерживает:
     - `min_price` - минимальная цена
     - `max_price` - максимальная цена
     - `min_discount` - минимальная скидка
     - `sort_by` - сортировка (discount, price_asc, price_desc, new)

2. **Избранное**
   - `GET /api/v1/favorites` - получить избранные товары
   - `POST /api/v1/favorites/add` - добавить в избранное
   - `POST /api/v1/favorites/remove` - удалить из избранного

3. **Корзина**
   - `GET /api/v1/cart/calculate` - рассчитать стоимость корзины

4. **Геолокация**
   - `POST /api/v1/stores/nearby` - найти ближайшие магазины

5. **Поиск**
   - `GET /api/v1/search/suggestions` - подсказки для автодополнения

6. **Статистика**
   - `GET /api/v1/stats/hot-deals` - общая статистика по акциям

### ✅ Обновлена база данных (`database.py`)

Добавлена новая таблица:
```sql
CREATE TABLE offer_favorites (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    offer_id INTEGER NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_id, offer_id)
)
```

Добавлены методы:
- `add_user_favorite(user_id, offer_id)` - добавить в избранное
- `remove_user_favorite(user_id, offer_id)` - удалить из избранного
- `get_user_favorite_offers(user_id)` - получить список ID избранных
- `is_offer_favorite(user_id, offer_id)` - проверить, в избранном ли

---

## 📋 Шаги для развертывания

### 1. Обновление базы данных

База данных обновится автоматически при первом запуске бота (таблица `offer_favorites` будет создана через `init_db()`).

Если используете PostgreSQL, выполните миграцию:
```bash
psql -U your_user -d your_database -f migrations/add_favorites_table.sql
```

### 2. Проверка API

Запустите бот и проверьте доступность endpoints:

```bash
# Локально
python bot.py

# Проверка health
curl http://localhost:8000/api/v1/health
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "service": "fudly-webapp-api",
  "version": "2.0"
}
```

### 3. Тестирование новых endpoints

#### Получить товары с фильтрами:
```bash
curl "http://localhost:8000/api/v1/offers?min_price=1000&max_price=50000&min_discount=30&sort_by=discount&limit=10"
```

#### Добавить в избранное:
```bash
curl -X POST http://localhost:8000/api/v1/favorites/add \
  -H "Content-Type: application/json" \
  -H "X-Telegram-Init-Data: YOUR_INIT_DATA" \
  -d '{"offer_id": 123}'
```

#### Получить избранное:
```bash
curl http://localhost:8000/api/v1/favorites \
  -H "X-Telegram-Init-Data: YOUR_INIT_DATA"
```

#### Рассчитать корзину:
```bash
curl "http://localhost:8000/api/v1/cart/calculate?offer_ids=1:2,3:1,5:3"
```
Формат: `offer_id:quantity,offer_id:quantity`

#### Получить статистику:
```bash
curl "http://localhost:8000/api/v1/stats/hot-deals?city=Ташкент"
```

---

## 🎨 Реализация фронтенда

### Структура проекта fudly-webapp

Рекомендуемая структура:

```
fudly-webapp/
├── src/
│   ├── pages/
│   │   ├── HomePage.tsx          # Главная страница
│   │   ├── FavoritesPage.tsx     # Страница избранного
│   │   ├── CartPage.tsx          # Корзина
│   │   └── OfferDetailPage.tsx   # Детальная страница товара
│   ├── components/
│   │   ├── OfferCard.tsx         # Карточка товара
│   │   ├── CategoryFilter.tsx    # Фильтр категорий
│   │   ├── SearchBar.tsx         # Поиск
│   │   ├── FilterModal.tsx       # Модальное окно фильтров
│   │   └── SkeletonLoader.tsx    # Скелетон загрузчик
│   ├── hooks/
│   │   ├── useInfiniteScroll.ts  # Бесконечная прокрутка
│   │   ├── useDebounce.ts        # Debounce для поиска
│   │   └── useFavorites.ts       # Работа с избранным
│   ├── api/
│   │   └── client.ts             # API клиент
│   ├── store/
│   │   ├── cartStore.ts          # Zustand/Redux для корзины
│   │   └── favoritesStore.ts     # Zustand/Redux для избранного
│   └── styles/
│       └── HomePage.css
├── public/
│   └── placeholder.png
├── package.json
├── vite.config.ts
└── tsconfig.json
```

### Установка зависимостей

```bash
cd fudly-webapp

# Если еще не установлены
npm install react react-dom
npm install -D @types/react @types/react-dom
npm install axios
npm install zustand  # Для управления состоянием
npm install @telegram-apps/sdk  # Telegram WebApp SDK
```

### Основные файлы

См. файл `docs/WEBAPP_IMPROVEMENTS.md` для полного кода компонентов.

### Конфигурация API

В `src/api/client.ts` укажите правильный URL вашего бэкенда:

```typescript
const API_BASE = import.meta.env.VITE_API_URL || 'https://your-bot-api.railway.app/api/v1';
```

Создайте `.env` файл:
```env
VITE_API_URL=https://your-bot-api.railway.app/api/v1
```

---

## 🔧 Интеграция с Telegram

### В файле бота (`handlers/common/webapp.py`)

URL уже настроен:
```python
WEBAPP_URL = "https://fudly-webapp.vercel.app"
```

### Настройка Bot Father

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/setmenubutton`
3. Выберите вашего бота
4. Отправьте текст кнопки: `🛍️ Открыть каталог`
5. Отправьте URL: `https://fudly-webapp.vercel.app`

### Настройка WebApp в меню

```bash
# Команда для BotFather
/mybots
# Выберите бота
# Menu Button > Edit menu button URL
# Введите: https://fudly-webapp.vercel.app
```

---

## 🚀 Деплой

### Backend (Railway)

Бэкенд автоматически обновится при следующем коммите:

```bash
git add .
git commit -m "Add webapp improvements: filters, favorites, cart"
git push origin main
```

Railway автоматически подхватит изменения.

### Frontend (Vercel)

```bash
cd fudly-webapp

# Сборка
npm run build

# Деплой
vercel deploy --prod
```

Или настройте автоматический деплой через GitHub:
1. Подключите репозиторий к Vercel
2. При каждом push в main ветку будет автоматический деплой

---

## 📊 Мониторинг и отладка

### Проверка логов на Railway

```bash
# В Railway Dashboard
# Выберите ваш сервис > Deployments > Logs
```

### Проверка работы API

```bash
# Health check
curl https://your-bot-api.railway.app/api/v1/health

# Получить категории
curl https://your-bot-api.railway.app/api/v1/categories

# Получить офферы
curl https://your-bot-api.railway.app/api/v1/offers?limit=5
```

### Отладка в Telegram

1. Откройте DevTools в Telegram Desktop
2. Нажмите `Ctrl+Shift+I` (Windows) или `Cmd+Option+I` (Mac)
3. Перейдите во вкладку Console
4. Проверьте ошибки JavaScript

---

## ✅ Чеклист готовности

- [ ] База данных обновлена (таблица `offer_favorites` создана)
- [ ] API endpoints работают (проверено через curl)
- [ ] Фронтенд собран и задеплоен на Vercel
- [ ] CORS настроен в `app/api/api_server.py` (добавлен Vercel домен)
- [ ] BotFather настроен с URL мини-приложения
- [ ] Тестирование в Telegram на реальных пользователях
- [ ] Мониторинг ошибок настроен (Sentry опционально)

---

## 🐛 Troubleshooting

### Проблема: CORS ошибки

**Решение:** Убедитесь что в `app/api/api_server.py` добавлен ваш Vercel домен:

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://fudly-webapp.vercel.app",
        "https://*.vercel.app",  # Все preview деплои
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### Проблема: Избранное не сохраняется

**Решение:** Проверьте что:
1. Таблица `offer_favorites` создана в БД
2. В заголовках запроса передается `X-Telegram-Init-Data`
3. Метод `add_user_favorite` вызывается в API

### Проблема: Фильтры не работают

**Решение:** Убедитесь что:
1. Параметры передаются в query string
2. На бэкенде правильно парсятся параметры
3. Логика фильтрации применяется после загрузки офферов

---

## 📱 Тестирование на устройствах

### iOS
- Safari (встроенный браузер Telegram)
- Проверьте работу touch событий
- Проверьте безопасные зоны (notch)

### Android
- Chrome (встроенный браузер Telegram)
- Проверьте работу pull-to-refresh
- Проверьте клавиатуру (input focus)

### Desktop
- Telegram Desktop (Windows, Mac, Linux)
- Web версия Telegram

---

## 📈 Метрики успеха

После запуска отслеживайте:

1. **Конверсия**
   - % пользователей, открывших каталог
   - % добавивших товары в корзину
   - % оформивших заказ

2. **Вовлеченность**
   - Среднее время в приложении
   - Количество просмотренных товаров
   - Использование фильтров и поиска

3. **Популярность функций**
   - Использование избранного
   - Использование корзины
   - Популярные категории

4. **Технические метрики**
   - Время загрузки страницы
   - Количество ошибок API
   - Процент успешных заказов

---

## 🎯 Следующие шаги

1. ✨ **Push-уведомления** о новых акциях
2. 📍 **Карта магазинов** с геолокацией
3. 💬 **Отзывы и рейтинги** товаров
4. 🎁 **Программа лояльности** с бонусами
5. 📊 **Аналитика** поведения пользователей
6. 🔔 **Напоминания** о забронированных товарах
7. 💳 **Онлайн-оплата** (Click, Payme, Uzum)

---

## 📞 Поддержка

Если возникли вопросы:
1. Проверьте логи в Railway
2. Проверьте Console в браузере
3. Создайте issue в GitHub репозитории

---

**Успешного запуска! 🚀**
