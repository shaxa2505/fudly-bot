# UX-анализ корзины и оформления заказа в Fudly Bot

## 1. As-is сценарий (покупатель, каталог → корзина → заказ)

### 1.1 Главное меню
- Кнопка "🛒 Корзина" / "🛒 Savat" ведёт в модуль `handlers.customer.features`, который перенаправляет в `handlers.customer.cart.show_cart`.

### 1.2 Каталог / офферы
- "🔥 Горячие предложения" и просмотр магазинов реализованы в `browse_hot.py` / `browse_stores.py`.
- В карточке товара есть inline-кнопка "✅ Добавить в корзину" (`add_to_cart_*`).
- После нажатия показывается карточка добавления в корзину:
  - Название, цена/скидка, количество, остаток, магазин, адрес и пр.
  - Кнопки: `➖ qty ➕`, "✅ Добавить в корзину", "❌ Отмена".

### 1.3 После подтверждения добавления
- В `handlers/customer/cart/add.py` товар кладётся в `cart_storage`.
- Пользователь получает подтверждающее сообщение и иногда кнопку "🛒 Перейти в корзину" / опцию продолжить покупки.

### 1.4 Просмотр корзины (`handlers/customer/cart/view.py`)
- При пустой корзине:
  - Текст: "🛒 Корзина пуста... / Savat bo'sh...".
  - Кнопка "🔥 Горячие предложения" (`hot_offers`).
- При наличии товаров:
  - Заголовок "🛒 Корзина / Savat".
  - Для каждого товара: название, qty×цена, магазин.
  - Итоговая сумма, опционально блок по доставке.
  - Клавиатура:
    - По строке на товар: "N. Название (qty)" (no-op) + "🗑" (`cart_remove_...`).
    - Ниже: "🏪 Самовывоз" (`cart_confirm_pickup`), "🚚 Доставка (+price)" (`cart_confirm_delivery`, если доступна доставка), "🗑 Очистить" (`cart_clear`).

### 1.5 Оформление pickup (`checkout.py`)
- Быстрый путь: жмут "🏪 Самовывоз" в корзине.
  - Проверка, что все товары из одного магазина.
  - Сбор `OrderItem` и вызов `UnifiedOrderService.create_order(..., order_type="pickup")`.
  - Очистка корзины, сейчас — только ответ `callback.answer("✅")`.
- Альтернативный путь: `cart_checkout` (если используется):
  - Показ сводки заказа (товары, сумма, доставка).
  - Кнопки: "Самовывоз" / "Доставка" и "◀️ Назад" (`back_to_cart`).

### 1.6 Оформление delivery (`delivery.py` + `payment.py`)
- `cart_confirm_delivery`:
  - Проверка минимальной суммы заказа для доставки.
  - Сохранение cart_items / store_id / delivery_price в FSM, установка `OrderDelivery.address`.
  - Просьба ввести адрес.
- Ввод адреса:
  - Валидация длины, сохранение в БД.
  - Показ сводки (товары, доставка, итог, адрес) и кнопок: "💳 Click", "💳 Карта/Karta", "🔙 Назад" (`cart_back_to_address`).
- `cart_pay_click_*`:
  - Создание заказа через `UnifiedOrderService`.
  - Очистка корзины.
  - Попытка выслать invoice через Telegram Payments, при ошибке — переключение на сценарий "карта".
- `cart_pay_card_*`:
  - Создание заказа (delivery, оплата картой).
  - Перевод FSM в `OrderDelivery.payment_proof`.
  - Сообщение с реквизитами карты и просьбой прислать скрин.
- `cart_payment_proof`:
  - Обновление статуса оплаты на `pending`, уведомление админа с inline-кнопками подтверждения/отклонения.
  - Сообщение пользователю о принятии заказа и ожидании подтверждения оплаты.

---

## 2. Главные UX‑узкие места и фрикции

1. **Два разных входа в оформление (быстрый и через `cart_checkout`)**
   - Для пользователя это выглядит как два немного разных сценария с похожими шагами.
   - Риск ощущаемой избыточности и путаницы.

2. **Быстрый pickup‑флоу без явного подтверждения / следующего шага**
   - После `cart_confirm_pickup` пользователь видит только `✅`, без номера заказа, деталей и CTA.
   - Возникает вопрос: «А что теперь? Где посмотреть заказ?».

3. **Перегруженность карточки добавления в корзину**
   - Много второстепенных деталей (скидка, срок годности, адрес и т.п.) для простого действия «добавить 1 штуку».
   - Это повышает когнитивную нагрузку, особенно на мобильном экране.

4. **Разные паттерны "назад"**
   - Используются разные иконки и тексты ("◀️ Назад", "🔙 Ortga", переходы через "Корзина").
   - Нет единой, предсказуемой модели возврата на предыдущий шаг.

5. **Немые состояния при ошибке зависимостей**
   - Если `db`/`bot` не проинициализированы или импортированы некорректно, хендлеры silently return и пользователь ничего не видит (например, баг с корзиной, когда `show_cart` просто выходил).

6. **Нет явного "обучения" корзине**
   - Пользователь сам догадывается, что нужно сделать, чтобы добавить/оформить.
   - Нет короткого сценария-подсказки при первом использовании.

---

## 3. JTBD и сегменты

- **JTBD:**
  - «Когда я хочу заказать еду/товары через Fudly‑бот, я хочу быстро собрать несколько позиций из одного магазина и быть уверен, что заказ создан и будет доставлен/готов к выдаче, чтобы не тратить время на звонки и уточнения».

- **Сегменты:**
  - Новые пользователи бота (важна обучаемость и простота).
  - Постоянные клиенты одного магазина (важна скорость и минимум шагов).
  - Пользователи, сравнивающие несколько магазинов (важно чётко обозначить ограничение "один заказ = один магазин").
  - Чувствительные к доставке (важна прозрачность min‑суммы и стоимости доставки).

---

## 4. Чек‑лист UX‑тестов (ручных/авто)

1. **Пустая корзина**
   - Открыть "🛒 Корзина" без товаров.
   - Ожидание: понятный текст + кнопка "🔥 Горячие предложения", которая работает.

2. **Добавление одного товара**
   - Из каталога нажать "✅ Добавить в корзину" и сразу подтвердить.
   - Ожидание: ясное подтверждение и простой путь в корзину.

3. **Просмотр корзины с одним товаром**
   - Ожидание: понятная шапка, список, итог, понятные CTA (оформить / удалить / очистить).

4. **Изменение количества и удаление**
   - Проверить кнопки `cart_qty_inc_`, `cart_qty_dec_`, `cart_remove_...`.
   - Ожидание: обновления без "тишины" и понятные ошибки при лимитах.

5. **Pickup‑оформление**
   - Из корзины нажать "🏪 Самовывоз".
   - Ожидание: понятное завершение с номером заказа и дальнейшими шагами.

6. **Delivery с min‑суммой**
   - Тест с суммой ниже min‑лимита доставки.
   - Ожидание: чёткое объяснение, что нужно сделать.

7. **Delivery: адрес → Click**
   - Проверка успешного инвойса и fallback на карту.

8. **Delivery: адрес → карта → скрин**
   - Проверка уведомления админа и финального сообщения пользователю.

9. **Навигация "назад" на всех шагах**
   - Проверка, что с любого шага понятен путь назад и пользователь не теряет контекст.

---

## 5. Первые рекомендации по улучшениям

1. Унифицировать входы в checkout: минимизировать различия между быстрым путём и `cart_checkout`, использовать единый экран подтверждения.
2. Добавить явное подтверждение после pickup‑оформления (номер заказа, краткая сводка, кнопки "Мои заказы" / "В меню").
3. Упростить карточку добавления в корзину для типичного кейса (можно оставить больше деталей во вторичном сценарии).
4. Стандартизировать паттерны "назад" (единые иконки и тексты для RU/UZ).
5. Жёстче валидировать зависимости (`db`, `bot`) и не допускать silent‑ошибок.
6. Добавить короткое обучающее сообщение при первом использовании корзины.
