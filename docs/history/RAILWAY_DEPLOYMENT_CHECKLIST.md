# ✅ Railway Deployment Checklist - Fudly Bot

## 🎯 Статус: Бот локально работает, нужна финальная настройка Railway

### 📋 Обязательные переменные окружения в Railway

Перейдите в Railway → Ваш проект → fudly-bot service → **Variables** и добавьте:

#### 1️⃣ TELEGRAM_BOT_TOKEN (ОБЯЗАТЕЛЬНО)
```
Variable: TELEGRAM_BOT_TOKEN
Value: [ваш токен от @BotFather]
```
❌ **БЕЗ ЭТОГО БОТ НЕ ЗАПУСТИТСЯ**

#### 2️⃣ ADMIN_ID (ОБЯЗАТЕЛЬНО)
```
Variable: ADMIN_ID
Value: [ваш Telegram ID, получить у @userinfobot]
```
Пример: `253445521`

#### 3️⃣ DATABASE_URL (ОБЯЗАТЕЛЬНО для PostgreSQL)
**Вариант A: Service Reference (рекомендуется)**
1. Нажмите **"+ New Variable"** → **"Add a Reference"**
2. Выберите ваш **PostgreSQL service**
3. Выберите **`DATABASE_URL`**
4. Railway создаст: `${{Postgres.DATABASE_URL}}`

**Вариант B: Ручная настройка**
```
Variable: DATABASE_URL
Value: postgresql://user:password@host:port/database
```
(скопируйте из PostgreSQL service → Variables → DATABASE_URL)

#### 4️⃣ USE_WEBHOOK (ОБЯЗАТЕЛЬНО)
```
Variable: USE_WEBHOOK
Value: true
```

#### 5️⃣ WEBHOOK_URL (ОБЯЗАТЕЛЬНО)
```
Variable: WEBHOOK_URL
Value: https://[ваш-app-name].up.railway.app
```
**Как найти:**
1. Railway → fudly-bot service → **Settings** → **Public Networking**
2. Скопируйте URL (например: `https://fudly-bot-production.up.railway.app`)

#### 6️⃣ PORT (Автоматически предоставляется Railway)
Railway обычно автоматически добавляет `PORT`, но если нет:
```
Variable: PORT
Value: 8080
```

### 🔧 Опциональные переменные

#### WEBHOOK_PATH
```
Variable: WEBHOOK_PATH
Value: /webhook
```
(По умолчанию: `/webhook`)

#### SECRET_TOKEN
```
Variable: SECRET_TOKEN
Value: [любая случайная строка 32+ символа]
```
(Для дополнительной безопасности webhook)

---

## 🚀 Порядок действий

### Шаг 1: Проверьте PostgreSQL
1. Railway → PostgreSQL service → **Убедитесь что статус "Active"**
2. PostgreSQL service → **Variables** → Проверьте наличие `DATABASE_URL`

### Шаг 2: Настройте переменные бота
1. Railway → fudly-bot service → **Variables**
2. Добавьте **все 6 обязательных переменных** (см. выше)
3. Используйте **Service Reference** для `DATABASE_URL`

### Шаг 3: Проверьте деплой
1. Railway → fudly-bot service → **Deployments**
2. Кликните на последний deployment → **View Logs**
3. Должны увидеть:
   ```
   🐘 Using PostgreSQL database
   ✅ PostgreSQL connection pool created
   ✅ Database initialized successfully
   ✅ Webhook установлен: https://...
   🌐 Webhook сервер запущен на порту 8080
   ```

### Шаг 4: Тестирование
1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Бот должен ответить приветствием

---

## 🐛 Troubleshooting

### Проблема: "Connection refused" к PostgreSQL
**Решение:**
- Используйте **Service Reference** для `DATABASE_URL`
- Не копируйте URL вручную, используйте reference: `${{Postgres.DATABASE_URL}}`

### Проблема: "BOT_TOKEN is not set"
**Решение:**
- Переменная должна называться `TELEGRAM_BOT_TOKEN` (не `BOT_TOKEN`)

### Проблема: "Failed to set webhook"
**Решение:**
- Проверьте `WEBHOOK_URL` - должен быть публичный Railway URL
- Убедитесь что `USE_WEBHOOK=true`
- Проверьте что Railway присвоил публичный домен (Settings → Public Networking → Generate Domain)

### Проблема: Бот не отвечает на команды
**Решение:**
1. Проверьте логи: Railway → Deployments → View Logs
2. Убедитесь что webhook установлен успешно
3. Проверьте что порт открыт: Railway автоматически маршрутизирует трафик на PORT

---

## 📊 Проверка текущего состояния

### ✅ Что работает:
- ✅ Код исправлен, все синтаксические ошибки устранены
- ✅ PostgreSQL connection pool работает
- ✅ Локальный запуск успешен
- ✅ Все NameError исправлены
- ✅ Webhook сервер запускается

### ❓ Что нужно проверить:
- ❓ Все ли переменные окружения добавлены в Railway?
- ❓ Используется ли Service Reference для DATABASE_URL?
- ❓ Установлен ли webhook успешно? (проверьте логи)
- ❓ Отвечает ли бот на команды в Telegram?

---

## 🎉 Когда всё работает:

Вы увидите в логах:
```
🐘 Using PostgreSQL database
✅ PostgreSQL connection pool created (min=1, max=10)
✅ PostgreSQL database schema initialized successfully
✅ Database initialized successfully
✅ Админ установлен: [ваш ID]
✅ Бот успешно запущен!
🔄 Режим: Webhook
✅ Webhook установлен: https://[ваш-url]/webhook
🌐 Webhook сервер запущен на порту 8080
```

И бот будет отвечать на все команды в Telegram! 🚀

---

## 📞 Быстрая справка по командам

### Для тестирования бота:
- `/start` - Начать работу с ботом
- `/menu` - Главное меню
- `/help` - Помощь

### Для админа (после установки ADMIN_ID):
- Отправьте `/start` и получите доступ к админ-панели
- Сможете модерировать магазины, товары, пользователей

---

## 📝 Итог

**Код полностью готов ✅**
**PostgreSQL подключается ✅**
**Локально работает ✅**

**Осталось:** Проверить переменные окружения в Railway и убедиться, что webhook установлен успешно.

После настройки всех переменных Railway автоматически перезапустит бот и всё заработает! 🎉
