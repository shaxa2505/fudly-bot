# 🧪 Локальное тестирование с Railway PostgreSQL

## Зачем это нужно?

- ✅ Видите ошибки **сразу** в консоли
- ✅ Можете **мгновенно** исправить и перезапустить
- ✅ Используете **настоящую БД** с Railway
- ✅ Тестируете как в **продакшене**
- ✅ Не нужно ждать деплой на Railway

---

## 🚀 Быстрый старт

### 1. Получите DATABASE_URL из Railway

```bash
# Откройте Railway Dashboard → Variables
# Скопируйте значение DATABASE_URL
```

### 2. Создайте/обновите `.env` файл

```bash
# .env
TELEGRAM_BOT_TOKEN=ваш_токен
ADMIN_ID=ваш_id
DATABASE_URL=postgresql://postgres:password@host.railway.app:5432/railway
USE_WEBHOOK=false
```

### 3. Запустите локальное тестирование

```powershell
# Активируйте виртуальное окружение
.\.venv\Scripts\Activate.ps1

# Запустите тест
python test_local.py
```

---

## 📋 Подробная инструкция

### Шаг 1: Подготовка

1. **Откройте Railway Dashboard**
   - Перейдите в ваш проект fudly-bot
   - Откройте вкладку "Variables"

2. **Скопируйте переменные:**
   ```
   DATABASE_URL
   TELEGRAM_BOT_TOKEN  
   ADMIN_ID
   ```

### Шаг 2: Настройка .env

Создайте файл `.env` в корне проекта:

```bash
# Database (из Railway)
DATABASE_URL=postgresql://postgres:xxxxx@containers-us-west-xxx.railway.app:5432/railway

# Bot credentials
TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather
ADMIN_ID=ваш_telegram_id

# Local settings
USE_WEBHOOK=false
PORT=8080
```

### Шаг 3: Запуск

```powershell
# Windows PowerShell

# 1. Активируйте venv
.\.venv\Scripts\Activate.ps1

# 2. Запустите тест
python test_local.py
```

---

## 🔍 Что вы увидите

```
================================================================================
🧪 ЛОКАЛЬНОЕ ТЕСТИРОВАНИЕ С RAILWAY DATABASE
================================================================================

✅ DATABASE_URL найден
📊 БД: containers-us-west-xxx.railway.app
🔧 Webhook отключен (используется polling)
✅ Bot token найден: ...your_token
👑 Admin ID: 123456789

================================================================================
🚀 Запускаю бот в режиме локального тестирования...
================================================================================

💡 ПОДСКАЗКИ:
   - Все ошибки будут видны в реальном времени
   - Используется PostgreSQL с Railway
   - Для остановки: Ctrl+C
   - Логи сохраняются в консоль

================================================================================

📁 Using SQLite database (local development)
💾 Using SQLite database with MemoryStorage
...
```

---

## 🐛 Как тестировать и исправлять ошибки

### Процесс:

1. **Запустите бот локально:**
   ```bash
   python test_local.py
   ```

2. **Нажмите кнопку в боте**

3. **Смотрите ошибку в консоли:**
   ```
   ❌ ERROR: psycopg2.errors.InvalidDatetimeFormat: invalid input syntax...
   File "/app/bot.py", line 638, in hot_offers_handler
   ```

4. **Исправьте код** (в VSCode)

5. **Остановите бот:** `Ctrl+C`

6. **Запустите снова:** `python test_local.py`

7. **Проверьте исправление**

8. **Если работает - коммитьте:**
   ```bash
   git add .
   git commit -m "Fix: описание исправления"
   git push
   ```

---

## 💡 Преимущества локального тестирования

### ❌ БЕЗ локального тестирования:
```
1. Нажимаете кнопку в боте
2. Ошибка на Railway
3. Открываете Railway Logs
4. Копируете ошибку
5. Исправляете код
6. git commit && git push
7. Ждёте деплой (1-2 минуты)
8. Проверяете - всё ещё не работает
9. Повторяете шаги 1-8...
⏱️ Время: 5-10 минут на одну ошибку
```

### ✅ С локальным тестированием:
```
1. Нажимаете кнопку в боте
2. Ошибка в консоли (сразу видно)
3. Исправляете код
4. Ctrl+C → python test_local.py (5 секунд)
5. Проверяете - работает!
6. git push (когда всё исправлено)
⏱️ Время: 30 секунд на одну ошибку
```

**Экономия времени: 10x быстрее!** ⚡

---

## 🔧 Полезные команды

### Быстрый перезапуск:
```powershell
# Остановить: Ctrl+C
# Запустить снова:
python test_local.py
```

### Проверка подключения к БД:
```python
# test_db.py
import os
from dotenv import load_dotenv
from database_pg import Database

load_dotenv()
db = Database()

# Проверка
stores = db.get_all_stores()
print(f"✅ Подключение работает! Найдено магазинов: {len(stores)}")
```

### Просмотр логов с фильтром:
```powershell
# Только ошибки
python test_local.py 2>&1 | Select-String "ERROR"

# Только WARNING и ERROR
python test_local.py 2>&1 | Select-String "ERROR|WARNING"
```

---

## 🎯 Workflow для исправления ошибок

### 1. Одна ошибка:
```bash
1. python test_local.py
2. Нажать кнопку
3. Увидеть ошибку
4. Ctrl+C
5. Исправить код
6. python test_local.py
7. Проверить - работает!
8. git commit && git push
```

### 2. Много ошибок:
```bash
1. python test_local.py
2. Тестировать ВСЕ кнопки
3. Записывать все ошибки
4. Ctrl+C
5. Исправить ВСЕ ошибки сразу
6. python test_local.py
7. Проверить всё - работает!
8. git commit && git push (один раз)
```

---

## 📊 Сравнение режимов

| Параметр | Railway (Production) | Local Testing |
|----------|---------------------|---------------|
| БД | PostgreSQL (Railway) | PostgreSQL (Railway) |
| Скорость запуска | 1-2 минуты | 5 секунд |
| Видимость ошибок | Через Logs | Сразу в консоли |
| Удобство отладки | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| Режим работы | Webhook | Polling |
| Для чего | Production | Development |

---

## ⚠️ Важные замечания

1. **Используйте одну и ту же БД**
   - Локально и на Railway - одна PostgreSQL
   - Все изменения сразу в production

2. **Будьте осторожны**
   - Вы работаете с настоящей БД
   - Не удаляйте данные без необходимости

3. **Тестируйте безопасно**
   - Используйте тестового пользователя
   - Создавайте тестовые товары
   - Помечайте тестовые данные

4. **Режим polling**
   - Локально используется polling (не webhook)
   - На Railway - webhook (быстрее)

---

## 🎓 Расширенные возможности

### Hot Reload (автоперезапуск при изменении кода):

```bash
# Установите watchdog
pip install watchdog

# Используйте
python -m watchdog.watchmedo auto-restart \
    --patterns="*.py" \
    --recursive \
    -- python test_local.py
```

### Детальное логирование:

```python
# В начале bot.py
import logging
logging.basicConfig(
    level=logging.DEBUG,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s'
)
```

---

## 🚀 Готовые команды

```powershell
# Полный цикл тестирования
.\.venv\Scripts\Activate.ps1; python test_local.py

# С проверкой БД
python test_db.py; python test_local.py

# С автоматической проверкой callback'ов
python check_callbacks.py; python test_local.py
```

---

## 📝 Чеклист перед push на Railway

- [ ] Протестировано локально
- [ ] Все кнопки работают
- [ ] Нет ошибок в консоли
- [ ] Проверено создание/удаление
- [ ] Проверена навигация
- [ ] Код закоммичен

```bash
# Только после этого:
git push origin main
```

---

**Теперь вы можете тестировать в 10 раз быстрее!** ⚡🚀
