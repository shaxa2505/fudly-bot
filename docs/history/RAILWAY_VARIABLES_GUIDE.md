# Пошаговая инструкция: Настройка Environment Variables в Railway

## Шаг 1: Откройте Railway Dashboard

1. Зайдите на https://railway.app
2. Авторизуйтесь (если ещё не вошли)
3. Найдите ваш проект `fudly-bot` в списке проектов

## Шаг 2: Откройте настройки проекта

1. Кликните на ваш проект
2. Вы увидите карточки сервисов (например, `fudly-bot-main` и `PostgreSQL`)
3. Кликните на карточку **вашего бота** (не PostgreSQL)

## Шаг 3: Перейдите во вкладку Variables

1. В открывшемся окне найдите вкладки вверху
2. Кликните на вкладку **"Variables"** (или "Переменные")
3. Вы увидите список текущих переменных окружения

## Шаг 4: Добавьте переменные

### Обязательные переменные:

#### 1. TELEGRAM_BOT_TOKEN
```
Название: TELEGRAM_BOT_TOKEN
Значение: <ваш токен от @BotFather>
```
**Как получить:**
- Откройте Telegram
- Найдите бота @BotFather
- Отправьте `/mybots` → выберите своего бота → API Token
- Скопируйте токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

#### 2. ADMIN_ID
```
Название: ADMIN_ID
Значение: <ваш Telegram ID>
```
**Как получить:**
- Откройте Telegram
- Найдите бота @userinfobot
- Отправьте `/start`
- Бот покажет ваш ID (например: `987654321`)

#### 3. USE_WEBHOOK
```
Название: USE_WEBHOOK
Значение: true
```
**Просто введите:** `true` (без кавычек)

#### 4. WEBHOOK_URL
```
Название: WEBHOOK_URL
Значение: https://ВАШЕ-ИМЯ-ПРОЕКТА.up.railway.app
```
**Как узнать:**
- В Railway Dashboard → вкладка **"Settings"** → секция **"Domains"**
- Там будет ваш домен (например: `fudly-bot-production.up.railway.app`)
- Скопируйте и добавьте `https://` в начало

#### 5. TELEGRAM_SECRET_TOKEN
```
Название: TELEGRAM_SECRET_TOKEN
Значение: <случайная строка>
```
**Сгенерируйте токен:**
Вариант 1 - онлайн:
- Откройте https://www.random.org/strings/
- Установите: длина 32, символы alphanumeric
- Нажмите "Get Strings"
- Скопируйте результат

Вариант 2 - в PowerShell:
```powershell
-join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | % {[char]$_})
```

### Рекомендуемые переменные:

#### 6. LOG_LEVEL
```
Название: LOG_LEVEL
Значение: INFO
```

#### 7. DB_MIN_CONN
```
Название: DB_MIN_CONN
Значение: 1
```

#### 8. DB_MAX_CONN
```
Название: DB_MAX_CONN
Значение: 10
```

#### 9. MAX_REQUESTS_PER_MINUTE
```
Название: MAX_REQUESTS_PER_MINUTE
Значение: 20
```

#### 10. RATE_LIMIT_WINDOW
```
Название: RATE_LIMIT_WINDOW
Значение: 60
```

## Шаг 5: Сохранение

- После добавления КАЖДОЙ переменной нажмите **"Add"** или Enter
- Railway автоматически сохраняет изменения
- После добавления всех переменных проект **автоматически перезапустится**

## Шаг 6: Проверка DATABASE_URL (автоматическая)

**ВАЖНО:** Переменная `DATABASE_URL` добавляется **автоматически** Railway при подключении PostgreSQL!

Чтобы убедиться:
1. Вкладка **"Variables"**
2. Найдите `DATABASE_URL` в списке
3. Она должна выглядеть как: `postgresql://postgres:PASSWORD@HOST:PORT/railway`
4. **НЕ ТРОГАЙТЕ её** - Railway управляет ей автоматически

## Шаг 7: Дождитесь перезапуска

1. После добавления переменных Railway автоматически редеплоит проект
2. Перейдите на вкладку **"Deployments"**
3. Дождитесь статуса **"Success"** (обычно 1-3 минуты)
4. Если есть ошибки - смотрите логи во вкладке **"Logs"**

---

## 🎯 Краткий чеклист:

- [ ] TELEGRAM_BOT_TOKEN (от @BotFather)
- [ ] ADMIN_ID (от @userinfobot)
- [ ] USE_WEBHOOK = true
- [ ] WEBHOOK_URL = https://ваш-домен.up.railway.app
- [ ] TELEGRAM_SECRET_TOKEN (случайная строка 32+ символа)
- [ ] LOG_LEVEL = INFO
- [ ] DB_MIN_CONN = 1
- [ ] DB_MAX_CONN = 10
- [ ] MAX_REQUESTS_PER_MINUTE = 20
- [ ] RATE_LIMIT_WINDOW = 60
- [ ] DATABASE_URL (✅ автоматически от Railway)

---

## ❓ Если что-то не работает:

### Проблема: Не вижу вкладку Variables
**Решение:** Убедитесь что кликнули на карточку **вашего бота**, а не на PostgreSQL

### Проблема: Бот не запускается после добавления переменных
**Решение:** 
1. Откройте вкладку **"Logs"**
2. Найдите ошибку в логах
3. Скорее всего неправильный TELEGRAM_BOT_TOKEN

### Проблема: DATABASE_URL отсутствует
**Решение:**
1. Убедитесь что PostgreSQL подключен к проекту
2. В Railway Dashboard → Add Service → Database → PostgreSQL
3. После создания DATABASE_URL появится автоматически

---

## 📸 Визуальная подсказка:

```
Railway Dashboard
├── Projects (список проектов)
│   └── fudly-bot (ваш проект)
│       ├── fudly-bot-main (карточка бота) ← КЛИКНИТЕ СЮДА
│       │   ├── Deployments
│       │   ├── Variables ← ЗДЕСЬ ДОБАВЛЯЙТЕ ПЕРЕМЕННЫЕ
│       │   ├── Logs
│       │   ├── Metrics
│       │   └── Settings
│       └── PostgreSQL (карточка БД)
│           └── Variables
│               └── DATABASE_URL ← УЖЕ ЕСТЬ (автоматически)
```

---

## ✅ После настройки переменных:

Переходите к следующим шагам из DEPLOYMENT_CHECKLIST.md:
- Шаг 4: Дождитесь успешного деплоя
- Шаг 5: Выполните миграцию БД
- Шаг 6: Установите webhook
