# 🔍 ПОЛНЫЙ АУДИТ СИСТЕМЫ ЗАКАЗОВ И ТОВАРОВ

**Дата:** 18 декабря 2025  
**Версия:** v19.0

---

## 📋 СОДЕРЖАНИЕ

1. [Аудит заказов в боте](#аудит-заказов-в-боте)
2. [Аудит заказов в партнер-панели](#аудит-заказов-в-партнер-панели)
3. [Аудит товаров](#аудит-товаров)
4. [Критические проблемы](#критические-проблемы)
5. [Рекомендации](#рекомендации)

---

## 1. АУДИТ ЗАКАЗОВ В БОТЕ

### 🏗️ Архитектура

**Сервисы:**
- `unified_order_service.py` (1470 строк) - главный сервис
- `order_service.py` (553 строки) - устаревший сервис

**Статусы:**
```python
PENDING     → Ожидание подтверждения продавца
PREPARING   → Продавец принял, готовит
READY       → Готов к выдаче/доставке
DELIVERING  → В пути (только доставка)
COMPLETED   → Завершен
REJECTED    → Отклонен продавцом
CANCELLED   → Отменен покупателем
```

### ✅ Что работает

1. **Unified Order Service**
   - Единая точка входа для всех операций
   - Поддержка pickup и delivery
   - Автоматические уведомления
   - Visual progress bars

2. **Типы заказов**
   - Одиночные товары (с карточки)
   - Корзина (множественные товары)
   - Самовывоз (pickup)
   - Доставка (delivery)

3. **Уведомления**
   - Минимизация спама
   - Прогресс-бары для отслеживания
   - Pickup: PREPARING, COMPLETED
   - Delivery: PREPARING, DELIVERING, COMPLETED

### ❌ Проблемы

#### 1. Дублирование кода
**Проблема:** Два сервиса делают одно и то же
- `unified_order_service.py` (новый)
- `order_service.py` (старый)

**Файлы:**
```
app/services/order_service.py         - 553 строки
app/services/unified_order_service.py - 1470 строк
```

**Риски:**
- Запутанность для разработчиков
- Возможны конфликты логики
- Сложность поддержки

**Решение:**
```python
# Удалить order_service.py
# Переименовать unified_order_service.py → order_service.py
# Обновить все импорты
```

#### 2. Legacy callbacks в bot.py

**Проблема:** Старый формат колбэков все еще поддерживается
```python
# bot.py строка 271-320
@fallback_router.callback_query(F.data.startswith("order_accept:"))
async def handle_order_accept(callback, db):
    # Старый формат: order_accept:{booking_code}:{customer_id}
    # Новый формат: order_confirm_{id}
```

**Файлы:**
- `bot.py` строки 271-320

**Решение:**
- Удалить legacy обработчики после миграции всех данных
- Оставить только unified формат

#### 3. Нет централизованного обработчика ошибок

**Проблема:** Ошибки обрабатываются по-разному в разных местах
```python
# bot.py строка 198-206
try:
    # ...
except Exception as e:
    await callback.answer("⚠️ Произошла ошибка")
    # Нет логирования деталей
    # Нет отправки в Sentry
```

**Решение:**
```python
from app.core.error_handler import handle_order_error

@handle_order_error
async def process_order(...):
    # Автоматическое логирование
    # Автоматическая отправка в Sentry
    # Красивое сообщение пользователю
```

#### 4. Нет валидации данных на уровне сервиса

**Проблема:** Валидация только в API, не в сервисе
```python
# unified_order_service.py
async def create_order(self, user_id, items, order_type, ...):
    # Нет проверки:
    # - user_id существует?
    # - items не пустой?
    # - order_type валидный?
    # - delivery_address есть для delivery?
```

**Решение:**
```python
from pydantic import BaseModel, validator

class CreateOrderRequest(BaseModel):
    user_id: int
    items: list[OrderItem]
    order_type: Literal["pickup", "delivery"]
    delivery_address: str | None
    
    @validator('items')
    def validate_items(cls, v):
        if not v:
            raise ValueError('Items cannot be empty')
        return v
    
    @validator('delivery_address')
    def validate_delivery(cls, v, values):
        if values.get('order_type') == 'delivery' and not v:
            raise ValueError('Delivery address required')
        return v
```

#### 5. Смешивание бизнес-логики и уведомлений

**Проблема:** Создание заказа и отправка уведомлений в одной функции
```python
# unified_order_service.py
async def create_order(...):
    # Создание заказа
    order_id = await db.insert(...)
    
    # Отправка уведомлений
    await self.notify_customer(...)
    await self.notify_seller(...)
    
    # Если уведомление упадет - заказ все равно создан
    # Но пользователь не узнает
```

**Решение:**
```python
# Разделить на 2 шага:
order = await order_service.create_order(...)
await notification_service.notify_order_created(order)

# Или использовать event-driven подход:
await event_bus.emit('order.created', order)
# → NotificationHandler автоматически обработает
```

#### 6. Хардкод текстов уведомлений

**Проблема:** Тексты зашиты в коде, нет i18n
```python
# unified_order_service.py строка 300
msg = f"🎉 <b>Ваш заказ принят!</b>\n\n"
msg += f"📦 <b>{self._esc(item.title)}</b>\n"
# Нет поддержки других языков
```

**Решение:**
```python
from app.core.i18n import _

msg = _("order.accepted.title")
msg += _("order.item", title=item.title)
```

#### 7. Отсутствует retry механизм

**Проблема:** Если уведомление не отправилось - оно потеряно навсегда
```python
try:
    await bot.send_message(...)
except Exception as e:
    logger.error(f"Failed to send: {e}")
    # Сообщение потеряно, retry нет
```

**Решение:**
```python
from app.core.retry import retry_with_backoff

@retry_with_backoff(max_attempts=3, backoff_seconds=5)
async def send_notification(...):
    await bot.send_message(...)
```

---

## 2. АУДИТ ЗАКАЗОВ В ПАРТНЕР-ПАНЕЛИ

### 🏗️ Архитектура

**Файлы:**
- `webapp/partner-panel/index.html` - фронтенд
- `app/api/partner_panel_simple.py` - API эндпоинты

**Эндпоинты:**
```python
GET  /api/partner/orders          - список заказов
POST /api/partner/orders/{id}/status  - обновить статус
POST /api/partner/orders/{id}/confirm - подтвердить
POST /api/partner/orders/{id}/cancel  - отменить
```

### ✅ Что работает (после v18.0)

1. **Визуальное различие статусов**
   - Цветные границы для каждого статуса
   - Новый: зеленая граница
   - Готовится: синяя
   - Готов: светло-зеленая
   - Завершен: серая
   - Отменен: серая + затухание

2. **Управление заказами**
   - Принять заказ (new → preparing)
   - Отметить готовым (preparing → ready)
   - Выдать клиенту (ready → completed)
   - Отменить на любом этапе

3. **Optimistic UI**
   - Мгновенное обновление интерфейса
   - Затухание карточки + пульсация
   - Смена цвета границы
   - Откат при ошибке

4. **Feedback**
   - Haptic вибрация
   - Visual feedback (иконки, анимации)
   - Toast уведомления
   - Блокировка кнопок
   - Confirm диалоги

### ❌ Проблемы

#### 1. Нет пагинации

**Проблема:** Все заказы загружаются сразу
```javascript
// index.html строка 746
async function loadOrders() {
    state.orders = await apiFetch('/api/partner/orders');
    // Если заказов 1000+ → долгая загрузка
    // Тормоза в UI
}
```

**Решение:**
```javascript
async function loadOrders(page = 1, limit = 20) {
    const response = await apiFetch(
        `/api/partner/orders?page=${page}&limit=${limit}`
    );
    state.orders = response.items;
    state.totalOrders = response.total;
    renderPagination(response.total, page, limit);
}
```

#### 2. Нет поиска и фильтрации

**Проблема:** Невозможно найти конкретный заказ
```javascript
// Нет функции поиска по:
// - ID заказа
// - Имени клиента
// - Телефону
// - Дате
```

**Решение:**
```html
<div class="search-filters">
    <input type="text" placeholder="Поиск по ID, имени, телефону..." 
           oninput="searchOrders(this.value)">
    <select onchange="filterByDate(this.value)">
        <option>Все время</option>
        <option>Сегодня</option>
        <option>Вчера</option>
        <option>Неделя</option>
    </select>
</div>
```

#### 3. Нет сортировки

**Проблема:** Заказы в случайном порядке
```javascript
// Нужна сортировка по:
// - Дате создания (новые сверху)
// - Статусу (новые → готовящиеся → готовые)
// - Сумме (большие → маленькие)
```

**Решение:**
```javascript
function sortOrders(by = 'created_at', order = 'desc') {
    state.orders.sort((a, b) => {
        if (by === 'created_at') {
            return order === 'desc' 
                ? new Date(b.created_at) - new Date(a.created_at)
                : new Date(a.created_at) - new Date(b.created_at);
        }
        // ...
    });
    renderOrders();
}
```

#### 4. Нет экспорта данных

**Проблема:** Невозможно выгрузить отчет
```javascript
// Нужен экспорт в:
// - CSV (для Excel)
// - PDF (для печати)
// - JSON (для анализа)
```

**Решение:**
```javascript
async function exportOrders(format = 'csv') {
    const data = await apiFetch(`/api/partner/orders/export?format=${format}`);
    const blob = new Blob([data], { type: 'text/csv' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = `orders_${Date.now()}.${format}`;
    a.click();
}
```

#### 5. Нет batch операций

**Проблема:** Нельзя выбрать несколько заказов и выполнить действие
```javascript
// Нужно:
// - Выбрать несколько заказов (чекбоксы)
// - Массово изменить статус
// - Массово отменить
// - Массово экспортировать
```

**Решение:**
```html
<div class="batch-actions" style="display: none;">
    <button onclick="batchUpdateStatus('preparing')">
        Принять выбранные (${selectedCount})
    </button>
    <button onclick="batchCancel()">Отменить выбранные</button>
</div>
```

#### 6. Нет звуковых уведомлений

**Проблема:** Партнер не слышит новые заказы
```javascript
// Нужно:
// - Звук при новом заказе
// - Desktop notification
// - Badge на иконке браузера
```

**Решение:**
```javascript
function playNotificationSound() {
    const audio = new Audio('/sounds/new-order.mp3');
    audio.play();
}

if (Notification.permission === 'granted') {
    new Notification('Новый заказ!', {
        body: `Заказ #${orderId} на ${price} ₽`,
        icon: '/icon.png'
    });
}
```

#### 7. API не возвращает полную информацию

**Проблема:** В ответе `/api/partner/orders` нет всех данных
```python
# partner_panel_simple.py строка 691
@router.get("/api/partner/orders")
async def list_orders(...):
    # Нет:
    # - customer_phone
    # - delivery_address
    # - payment_method
    # - created_at (timestamp)
    # - updated_at
```

**Решение:**
```python
@router.get("/api/partner/orders")
async def list_orders(...):
    orders = await db.fetch_all("""
        SELECT 
            o.*,
            u.phone as customer_phone,
            u.name as customer_name,
            o.delivery_address,
            o.payment_method,
            o.created_at,
            o.updated_at
        FROM orders o
        LEFT JOIN users u ON o.user_id = u.id
        WHERE o.store_id = ?
        ORDER BY o.created_at DESC
    """, [store_id])
    return orders
```

---

## 3. АУДИТ ТОВАРОВ

### 🏗️ Архитектура

**Файлы:**
- `webapp/partner-panel/index.html` - фронтенд (строки 1084-1450)
- `webapp/partner-panel/styles/products.css` - стили
- `app/api/partner_panel_simple.py` - API

**Эндпоинты:**
```python
GET    /api/partner/products          - список товаров
POST   /api/partner/products          - создать товар
PUT    /api/partner/products/{id}     - обновить товар
DELETE /api/partner/products/{id}     - удалить товар
POST   /api/partner/products/{id}/availability - скрыть/показать
```

### ✅ Что работает (после v19.0)

1. **Загрузка фото**
   - File input с drag&drop
   - Валидация (только изображения, макс 5MB)
   - Preview загруженного фото
   - Base64 конвертация

2. **URL фото**
   - Ввод URL в отдельное поле
   - Автоматический preview
   - Валидация формата

3. **Форма добавления**
   - Название* (обязательно, мин 3 символа)
   - Описание (макс 500 символов)
   - Цена* (обязательно, > 0)
   - Скидка (0-99%)
   - Остаток
   - Единица (шт, кг, л, г, мл)
   - Доступность (checkbox)

4. **Валидация**
   - Inline валидация при вводе
   - Красная/зеленая подсветка
   - Сообщения об ошибках

5. **Сетка товаров**
   - 2 колонки на десктопе
   - 1 колонка на мобильном
   - Удобство при большом количестве

### ❌ Проблемы

#### 1. Нет категорий

**Проблема:** Все товары в одной куче
```javascript
// Нужны категории:
// - Молочка
// - Хлеб
// - Овощи
// - Фрукты
// - Напитки
```

**Решение:**
```html
<select name="category_id">
    <option value="1">Молочка</option>
    <option value="2">Хлеб</option>
    <option value="3">Овощи</option>
    <!-- ... -->
</select>
```

#### 2. Фото загружается как base64 в БД

**Проблема:** Base64 раздувает размер БД
```javascript
// handlePhotoUpload строка 1320
reader.onload = (e) => {
    const photoUrl = e.target.result; // base64, ~1MB для 1 фото
    document.querySelector('input[name="photo_url"]').value = photoUrl;
};
```

**Решение:**
```javascript
// Загружать на CDN/S3
async function uploadPhoto(file) {
    const formData = new FormData();
    formData.append('photo', file);
    
    const response = await fetch('/api/partner/upload-photo', {
        method: 'POST',
        body: formData
    });
    
    const { url } = await response.json();
    return url; // https://cdn.example.com/photos/abc123.jpg
}
```

#### 3. Нет bulk создания товаров

**Проблема:** Нельзя добавить 10+ товаров за раз
```javascript
// Нужен CSV импорт:
// Название,Цена,Остаток,Единица,Фото URL
// Молоко,100,50,шт,https://...
// Хлеб,50,30,шт,https://...
```

**Решение:**
```html
<input type="file" accept=".csv" onchange="importProducts(this.files[0])">

<script>
async function importProducts(file) {
    const text = await file.text();
    const rows = text.split('\n').slice(1); // Skip header
    
    for (const row of rows) {
        const [name, price, stock, unit, photoUrl] = row.split(',');
        await createProduct({ name, price, stock, unit, photoUrl });
    }
}
</script>
```

#### 4. Нет bulk редактирования

**Проблема:** Нельзя изменить цену у 10+ товаров сразу
```javascript
// Нужно:
// - Выбрать товары (чекбоксы)
// - Изменить цену: +10%
// - Изменить скидку: 20%
// - Скрыть все выбранные
```

**Решение:**
```html
<div class="bulk-edit">
    <select onchange="bulkAction(this.value)">
        <option>Массовые действия</option>
        <option value="increase_price">Увеличить цену на %</option>
        <option value="set_discount">Установить скидку %</option>
        <option value="hide_all">Скрыть все</option>
    </select>
</div>
```

#### 5. Нет истории изменений

**Проблема:** Неизвестно кто и когда менял товар
```javascript
// Нужна таблица product_history:
// - product_id
// - user_id (кто изменил)
// - field (что изменил: price, stock, etc)
// - old_value
// - new_value
// - created_at
```

**Решение:**
```python
@router.put("/api/partner/products/{id}")
async def update_product(id, data, user_id):
    old = await db.fetch_one("SELECT * FROM products WHERE id = ?", [id])
    
    # Сохранить историю
    for field, new_value in data.items():
        if old[field] != new_value:
            await db.execute("""
                INSERT INTO product_history 
                (product_id, user_id, field, old_value, new_value)
                VALUES (?, ?, ?, ?, ?)
            """, [id, user_id, field, old[field], new_value])
    
    # Обновить товар
    await db.execute("UPDATE products SET ... WHERE id = ?", [id])
```

#### 6. Нет аналитики по товарам

**Проблема:** Непонятно какие товары популярные
```javascript
// Нужна аналитика:
// - Просмотры
// - Добавления в корзину
// - Покупки
// - Конверсия (покупки / просмотры)
// - Средний чек
```

**Решение:**
```python
@router.get("/api/partner/products/{id}/analytics")
async def product_analytics(id):
    views = await db.fetch_val("SELECT COUNT(*) FROM product_views WHERE product_id = ?", [id])
    cart_adds = await db.fetch_val("SELECT COUNT(*) FROM cart_items WHERE offer_id = ?", [id])
    purchases = await db.fetch_val("SELECT COUNT(*) FROM order_items WHERE offer_id = ?", [id])
    
    return {
        "views": views,
        "cart_adds": cart_adds,
        "purchases": purchases,
        "conversion": purchases / views if views else 0
    }
```

#### 7. Нет связи товар-заказы

**Проблема:** Нельзя посмотреть заказы конкретного товара
```javascript
// При клике на товар показать:
// - Список заказов с этим товаром
// - Кто заказывал
// - Когда заказывали
// - Сколько раз
```

**Решение:**
```javascript
async function viewProductOrders(productId) {
    const orders = await apiFetch(`/api/partner/products/${productId}/orders`);
    
    // Показать модалку со списком заказов
    showModal({
        title: 'Заказы товара',
        content: renderOrdersList(orders)
    });
}
```

---

## 4. КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 🔴 P0 (Блокирующие)

1. **Дублирование order сервисов**
   - Риск конфликтов
   - Запутанность кода
   - **Решение:** Удалить старый сервис

2. **Base64 фото в БД**
   - Раздувает БД
   - Медленные запросы
   - **Решение:** Загружать на CDN

3. **Нет пагинации заказов**
   - При 1000+ заказов панель зависает
   - **Решение:** Добавить пагинацию

### 🟠 P1 (Критические)

4. **Нет валидации в сервисах**
   - Можно создать заказ с пустыми items
   - **Решение:** Добавить Pydantic модели

5. **Нет retry для уведомлений**
   - Потеря уведомлений при сбоях
   - **Решение:** Retry механизм

6. **Смешивание логики и уведомлений**
   - Нарушение SOLID
   - **Решение:** Event-driven подход

### 🟡 P2 (Важные)

7. **Нет поиска заказов**
   - Сложно найти конкретный заказ
   - **Решение:** Добавить поиск

8. **Нет категорий товаров**
   - Хаос при большом ассортименте
   - **Решение:** Добавить категории

9. **Нет аналитики**
   - Непонятно что популярное
   - **Решение:** Добавить аналитику

---

## 5. РЕКОМЕНДАЦИИ

### Немедленные действия (эта неделя)

1. ✅ **Изменить сетку товаров на 2 колонки** ← СДЕЛАНО
2. ⏳ Удалить `order_service.py` (старый)
3. ⏳ Добавить пагинацию заказов (20 per page)
4. ⏳ Добавить поиск по заказам

### Краткосрочные (2 недели)

5. ⏳ Переместить фото на CDN
6. ⏳ Добавить валидацию в сервисах
7. ⏳ Добавить retry для уведомлений
8. ⏳ Добавить категории товаров

### Среднесрочные (месяц)

9. ⏳ Event-driven архитектура
10. ⏳ Аналитика товаров
11. ⏳ Bulk операции
12. ⏳ Экспорт данных

### Долгосрочные (квартал)

13. ⏳ i18n для всех текстов
14. ⏳ История изменений
15. ⏳ Advanced аналитика
16. ⏳ ML рекомендации

---

## 📊 МЕТРИКИ

### Текущее состояние

- **Файлов кода:** 83
- **Строк в order сервисах:** 2023 (553 + 1470)
- **Дублирование:** ~27% (553/2023)
- **Технический долг:** Высокий

### После исправлений

- **Файлов кода:** 82 (-1)
- **Строк кода:** 1470 (-553)
- **Дублирование:** 0%
- **Технический долг:** Средний

---

## ✅ ВЫВОДЫ

1. **Система работает**, но есть технический долг
2. **Партнер-панель v18.0** хорошо справляется с базовыми задачами
3. **Товары v19.0** имеют хороший UX для добавления
4. **Главные проблемы:** дублирование кода, отсутствие пагинации, base64 фото
5. **Сетка товаров:** ✅ изменена на 2 колонки

**Приоритет:** Сначала исправить P0 проблемы, затем P1, потом P2.

