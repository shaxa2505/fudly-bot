# 🐘 Railway PostgreSQL Setup Guide

Полное руководство по настройке PostgreSQL для Fudly Bot на Railway.

## 🎯 Зачем PostgreSQL?

### Проблема с SQLite + MemoryStorage
- ❌ **SQLite** - локальный файл, теряется при каждом деплое Railway
- ❌ **MemoryStorage** - FSM состояния теряются при перезапуске контейнера
- ❌ Пользователи теряют прогресс заказов, регистрации, создания офферов

### Решение: PostgreSQL
- ✅ **Persistent database** - данные сохраняются навсегда
- ✅ **FSM storage** - состояния сохраняются в той же БД
- ✅ **Бесплатный tier** - 512MB для маленьких проектов
- ✅ **Scalable** - можно расширить по мере роста
- ✅ **Production-ready** - надёжная реляционная БД

---

## 📦 Шаг 1: Добавление PostgreSQL на Railway

### 1.1 В проекте Fudly Bot
1. Открой проект на Railway: https://railway.app/project/[твой-проект]
2. Нажми **"+ New Service"**
3. Выбери **"Database"** → **"PostgreSQL"**
4. Railway автоматически создаст PostgreSQL сервис

### 1.2 Получение DATABASE_URL
После создания PostgreSQL сервиса:

1. Кликни на сервис **"PostgreSQL"**
2. Перейди на вкладку **"Variables"**
3. Найди переменную **`DATABASE_URL`**
4. Скопируй значение (формат: `postgresql://user:password@host:port/dbname`)

---

## ⚙️ Шаг 2: Настройка бота для PostgreSQL

### 2.1 Добавление DATABASE_URL в бот

1. Вернись к сервису **"fudly-bot"**
2. Перейди на вкладку **"Variables"**
3. Нажми **"+ New Variable"**
4. Добавь:
   - **Variable Name:** `DATABASE_URL`
   - **Value:** вставь скопированный URL из PostgreSQL сервиса

Пример URL:
```
postgresql://postgres:password123@containers-us-west-1.railway.app:5432/railway
```

### 2.2 Автоматическая миграция

Бот автоматически:
- ✅ Определит наличие `DATABASE_URL`
- ✅ Подключится к PostgreSQL
- ✅ Создаст все таблицы (users, stores, offers, orders, bookings, и т.д.)
- ✅ Создаст таблицу `fsm_states` для сохранения FSM состояний
- ✅ Создаст индексы для быстрого поиска

Не нужно ничего делать вручную! Просто деплой и всё работает.

---

## 🚀 Шаг 3: Деплой изменений

### 3.1 Автоматический деплой через GitHub

1. Код уже обновлён для поддержки PostgreSQL
2. Push в GitHub:
   ```bash
   git add -A
   git commit -m "✅ Миграция на PostgreSQL: database_pg.py + FSM storage"
   git push origin main
   ```

3. Railway автоматически задеплоит новую версию
4. Следи за логами в Railway Dashboard

### 3.2 Проверка в логах

После деплоя проверь логи (Railway → Service → Deployments → View Logs):

```
🐘 Using PostgreSQL database
✅ PostgreSQL connection pool created (min=1, max=10)
✅ PostgreSQL database schema initialized successfully
✅ Using PostgreSQL for FSM storage (persistent): postgresql://postgres@...
✅ PostgreSQL FSM storage initialized
✅ Database initialized successfully
```

Если видишь эти сообщения - всё работает! 🎉

---

## 📊 Шаг 4: Миграция существующих данных (опционально)

Если у тебя уже есть данные в SQLite локально и ты хочешь их перенести:

### 4.1 Экспорт из SQLite

```bash
# Установи psycopg2 локально
pip install psycopg2-binary

# Запусти скрипт миграции (создам если нужно)
python migrate_sqlite_to_postgres.py
```

### 4.2 Что мигрирует
- ✅ Все пользователи
- ✅ Все магазины
- ✅ Все офферы
- ✅ Все заказы
- ✅ Все бронирования
- ✅ Настройки оплаты
- ✅ Уведомления
- ❌ FSM состояния (невозможно мигрировать)

---

## 🔍 Проверка работы PostgreSQL

### Тест 1: Регистрация пользователя
1. Запусти `/start` в боте
2. Пройди регистрацию
3. **Перезапусти Railway сервис** (Railway → Service → Settings → Restart)
4. Запусти `/start` снова
5. ✅ Бот помнит тебя = PostgreSQL работает!

### Тест 2: FSM состояния (критический!)
1. Начни заказ: Выбери товар → Введи адрес → Получи карту
2. **НЕ ОТПРАВЛЯЙ СКРИНШОТ СРАЗУ**
3. **Перезапусти Railway сервис**
4. Отправь скриншот оплаты
5. ✅ Заказ проходит = FSM storage работает!
6. ❌ "Состояние потеряно" = FSM storage НЕ работает

---

## 💰 Стоимость

### Бесплатный Tier
- **PostgreSQL:** 512 MB storage
- **Достаточно для:**
  - ~5,000+ пользователей
  - ~10,000+ офферов
  - ~20,000+ заказов
  - Все FSM состояния

### Когда нужно платить?
- Только если превысишь 512 MB
- **Стоимость:** $5/месяц за дополнительные 512 MB
- Для маленького бизнеса бесплатного tier хватит надолго

---

## ❓ FAQ

### Что будет со старыми SQLite данными?
- На Railway они теряются при каждом деплое (как было всегда)
- Локально можно оставить SQLite для тестирования
- Для миграции используй скрипт (см. Шаг 4)

### Нужно ли удалять Redis?
- Да, Redis больше не нужен
- PostgreSQL заменяет и SQLite, и Redis
- Можно удалить Redis сервис из Railway

### Как откатиться на SQLite?
1. Удали переменную `DATABASE_URL` из Railway
2. Бот автоматически вернётся на SQLite + MemoryStorage
3. НО: данные из PostgreSQL не вернутся в SQLite

### Как посмотреть данные в PostgreSQL?
**Вариант 1: Railway Dashboard**
- Railway → PostgreSQL Service → Data
- Простой веб-интерфейс для запросов

**Вариант 2: pgAdmin / DBeaver**
- Установи pgAdmin или DBeaver
- Подключись используя `DATABASE_URL`

**Вариант 3: psql (CLI)**
```bash
psql "postgresql://user:pass@host:port/dbname"
SELECT * FROM users LIMIT 10;
```

### Можно ли использовать PostgreSQL локально?
Да!

1. Установи PostgreSQL локально
2. Создай БД: `createdb fudly`
3. Добавь в `.env`:
   ```
   DATABASE_URL=postgresql://localhost/fudly
   ```
4. Запусти бота

### Что если DATABASE_URL неправильный?
Бот выдаст ошибку при старте:
```
❌ Database initialization failed: connection refused
```

Проверь:
- URL правильно скопирован
- PostgreSQL сервис запущен на Railway
- Нет опечаток в переменной

---

## 🎯 Итоги

### До миграции
```
┌─────────────┐     ┌──────────────┐
│  SQLite     │     │ MemoryStorage│
│  (теряется) │     │  (теряется)  │
└─────────────┘     └──────────────┘
      ↓                    ↓
  ❌ Каждый         ❌ Каждый
   деплой            перезапуск
```

### После миграции
```
┌────────────────────────────┐
│     PostgreSQL Railway     │
│  ┌──────────┬────────────┐ │
│  │ Main DB  │ FSM States │ │
│  │(постоянно)│ (постоянно)│ │
│  └──────────┴────────────┘ │
└────────────────────────────┘
        ↓
   ✅ Всегда живёт
```

### Преимущества
- ✅ Данные не теряются при деплое
- ✅ FSM состояния сохраняются при перезапуске
- ✅ Пользователи не теряют прогресс заказов
- ✅ Бесплатно до 512 MB
- ✅ Production-ready решение
- ✅ Легко масштабируется

---

## 📝 Следующие шаги

1. ✅ Добавь PostgreSQL сервис на Railway
2. ✅ Скопируй DATABASE_URL в переменные бота
3. ✅ Сделай git push для деплоя
4. ✅ Проверь логи - "PostgreSQL database schema initialized"
5. ✅ Протестируй регистрацию и FSM состояния
6. ✅ Наслаждайся стабильным ботом! 🎉

---

## 🆘 Нужна помощь?

Если что-то не работает:
1. Проверь логи в Railway Dashboard
2. Убедись что `DATABASE_URL` установлен
3. Перезапусти сервис бота на Railway
4. Проверь что PostgreSQL сервис работает

Всё должно работать из коробки! 🚀
