# ✅ ИСПРАВЛЕНО: Автоматическое определение режима партнера при старте

**Проблема:** Партнеры видели меню покупателя, хотя являются партнерами

**Причина:** `user_view_mode` не инициализировался при старте бота

---

## 🔧 Решение

Добавлена автоматическая инициализация `user_view_mode` в 2 местах:

### 1. Команда `/start`
```python
# Initialize user_view_mode based on user role
from handlers import common_user
if common_user.user_view_mode is not None and user_role == 'seller':
    # If user is seller, default to seller mode
    common_user.user_view_mode[message.from_user.id] = 'seller'
```

### 2. Callback `back_to_menu`
```python
# Initialize user_view_mode for sellers
from handlers import common_user
if common_user.user_view_mode is not None and user_role == 'seller':
    # If not set, default to seller mode for sellers
    if callback.from_user.id not in common_user.user_view_mode:
        common_user.user_view_mode[callback.from_user.id] = 'seller'
```

---

## 🎯 Теперь работает так:

1. **Партнер запускает бота** → автоматически устанавливается режим партнера
2. **Партнер возвращается в меню** → проверяется и устанавливается режим партнера
3. **Партнер может переключиться** → на режим покупателя и обратно

---

## ✅ Что проверить:

1. Перезапустите бота
2. Отправьте `/start`
3. Должны увидеть меню партнера: ➕ Добавить, 📦 Мои товары, etc.

---

**Файл изменен:** `handlers/user_commands.py`
